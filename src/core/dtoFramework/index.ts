// Phase 15P A04 — public surface of the versioned-DTO framework: shared
// types, the dispatch/compatibility registry, and the built-in durable-DTO
// kind registrations (semantic receipt, replay plan v1/v2, campaign
// manifest, campaign checkpoint).
export * from './types';
export * from './registry';
export * from './builtinRegistrations';
