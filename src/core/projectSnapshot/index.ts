// ---------------------------------------------------------------------------
// Nightwatch Phase 15P (parallel agent A12) — deterministic project snapshot.
// Public surface: types, builder/serializer, comparator.
// Pure modules only; construction takes explicit caller-supplied values.
// ---------------------------------------------------------------------------

export * from './types';
export * from './build';
export * from './compare';
