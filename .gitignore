# dependencies
node_modules/

# build output
dist/

# test runner output
test-results/
test-results-*/

# run artifacts (evidence is never committed)
artifacts/*
!artifacts/.gitkeep

# owner-only private findings should remain outside Git even if a test/operator
# explicitly injects a repository-local root
.nightwatch/

# test fixtures / scratch
.tmp-*/
.tmp-test/

# secrets
.env
*.local.json

# auth/session files — secret material, never committed (defensive; Phase 1.1)
*.storage-state.json
*.storage-state
storage-state*.json
storageState*.json
auth-state*.json
auth*.json
session*.json
*.cookies.json
cookies*.json
*credentials*.json
credentials/
.secrets/
*.token
*.tokens.json

# misc
*.log
.DS_Store
