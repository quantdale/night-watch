# C-10.5 Provenance and Project-Truth Closure — Report

Status: IN_PROGRESS. This report is completed at campaign closeout; the
authoritative running record is `STATE.md`.

## 1. Starting SHA

`cb631cc4af3c3572f4cbf78da04a8265075fbfa5`, verified as both local HEAD and
`origin/main` with a clean tree and a single (canonical) worktree before any
modification.

## 2. Provenance-forgery reproduction

Recorded in `openspec/changes/nightwatch-c10-provenance-truth-closure-v1/audit.md`
with captured output. Summary: label-only minting succeeded for both
vocabularies, and a shape-matching JSON revival was accepted by the consumer.

## 3. Root cause

Pending closeout.

## Remaining report sections

Sections 4–14 of the Stage-A report (route/key authority before and after,
source binding mechanism, digest binding inputs, project-state drift and
validator repair, C-10 certification reconciliation, persisted-position
sentinel rule, regression totals, local/clean/CI receipts, final Stage-A SHA)
are written at closeout from the recorded `STATE.md` results.
