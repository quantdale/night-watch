# DEFECT LEDGER — Phase 16H

Populate only from observed hardening failures. Hypotheses are not defects until reproduced.

| ID | Gate | Reproducer | Observed failure | Root cause | Source fix | Permanent regression | Narrow recheck | Broad recheck | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEF-01 | G-floor starvation metric | phase16hPortfolioHardening.test.ts "G-floor baseline starvedEligible uses the policy threshold like optimized side": zero-budget portfolio with members aged 1 and 6 buckets (threshold 5). | baseline.starvedEligibleMembers returned 2 (young member counted); optimized side returned 1. Expected 1/1. Cross-side metric computed with different predicates -> incoherent starvation accounting. | simulator.ts simulateBaselineSide filtered `starvationAgeBuckets >= 0` (vacuous) while optimized-side starvedCount() used `>= starvationThresholdBuckets`. | Thread policy.starvationThresholdBuckets into simulateBaselineSide and use the threshold predicate. | "G-floor baseline starvedEligible..." + J02 determinism/floor runner over full corpus x3 | focused suite rerun | campaign:synthetic + compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-02 | F-matrix replan fail-closed | phase16hPortfolioHardening.test.ts F-row tests: trio portfolio funding top-2 of 3 members; CONTRACT_CHANGED / DERIVATION_CHANGED computed on the UNSELECTED member via classifyPortfolioSourceMovement vs baseline provenance; classifyPlanReplan called with that movement map. | Verdict was PLAN_REUSABLE, rebuildRequired=false, reasons=[NO_MATERIAL_EVIDENCE_CHANGE] despite the strongest movement signals firing anywhere in the universe. Also EVIDENCE_CHANGED on an unselected member polluted affectedSelectedMemberIds with an unselected id although the field is documented as selected-memberIds. | replan.ts classifyPlanReplan loop 3 skipped invalidating movement rules entirely (`if (!rule \|\| rule.invalidate) continue;`) contradicting its own comment ("unselected-member evidence changes ... require reprioritization"); and added unselected memberIds to the affected set. | Loop 3 now applies ANY material movement rule found on unselected members as a reprioritization reason (never invalidates valid selections) and no longer mutates the affected-selected set. | 3 dedicated F-row tests + REPLAN_MATRIX rows contract-change-unselected-member / derivation-change-unselected-member | focused suite rerun | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-03 | E01 manifest strictness | phase16hPortfolioHardening.test.ts "compare-plan rejects empty-object manifests..." + tamper matrix: comparePlanManifests({}, {}) and mutated manifests were accepted. | comparePlanManifests consumed arbitrary JSON: empty objects produced garbage-but-deterministic findings instead of failing closed; no strict parser existed for rendered plan documents (E01 gap). | No validation layer existed between JSON.parse output and the comparison logic; bin/portfolio.mjs handed readJson() results straight through. | Add strict parseCampaignPlanManifestDocument (exact keys, pinned versions, structural field validation, planId+manifestDigest recomputation match) in src/core/portfolio/manifest.ts; comparePlanManifests validates both inputs through it; CLI compare-plan uses the parser. | E01/E04 parser acceptance + 10-case MANIFEST_TAMPER_CASES rejection test + CLI compare-plan failure-path test | focused suite rerun | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-04 | H8 CLI sanitized errors | planned CLI reproducer: malformed --input JSON whose parse error echoes file-content fragments (e.g. sentinel-bearing text) into stderr. | bin/portfolio.mjs interpolated raw `error.message` from JSON.parse/fs errors into stderr output; top-level catch echoed arbitrary module messages unbounded. | Error surfaces had no bounded categorical sanitizer. | Add sanitizeDetail() (forbidden-shape redaction first, then 160-char bound) applied to every error echo path incl. top-level catch; compare-plan path additionally routed through the strict parser. | phase16hCliHardening.test.ts sanitized-failure cases (sentinel file, malformed JSON, unknown subcommand) | focused CLI suite rerun | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-04 | H8 CLI sanitized errors | tests/unit/phase16hCliHandoffHardening.test.ts "malformed sentinel-bearing input produces sanitized failure only": hostile JSON file with `Bearer abcsecretcookie` inside approvedTargets; also missing-file path echo observed in H04 missingRun. | bin/portfolio.mjs interpolated raw `error.message` from JSON.parse/fs errors into stderr; top-level catch echoed arbitrary module messages unbounded. | Error surfaces had no bounded categorical sanitizer; read failures echoed absolute paths incl. ENOENT details. | Add sanitizeDetail() (forbidden-shape redaction FIRST on raw text, then 160-char bound); read failures fully categorical (`cannot read --input` / `cannot read plan document`); parse failures carry bounded sanitized detail; top-level catch sanitizes too. | phase16hCliHandoffHardening.test.ts DEF-04/H04/H01 failure-path cases | focused CLI suite rerun GREEN | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-05 | G03 simulator model validation | phase16hPortfolioHardening.test.ts SIM_MODEL_EDGES x8: models with negative/NaN/infinite saturationCap, negative halfSaturation, waste outside 0..1000, non-boolean yieldsNothing. | runShadowSimulation accepted garbage models and fabricated metrics (e.g. negative usefulCandidates) instead of failing closed. | No shape validation existed for MemberYieldModel values; only MISSING-model was checked. | Add validateMemberYieldModel (integer>=0 cap/halfSaturation, integer duplicateWastePermille within 0..1000, boolean yieldsNothing) enforced in runShadowSimulation before any computation. | G03 model-edge test (all 8 edges throw PORTFOLIO_SIM_MODEL_INVALID) | focused suite rerun GREEN | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |
| DEF-06 | E01/H4 parser error contract (self-caught) | phase16hCliHandoffHardening.test.ts H04 tampered-manifest run initially printed `portfolio: invalid plan document:` with EMPTY detail; direct core probe showed thrown value was a bare string ("PLAN_MANIFEST_INVALID:totalAllocatedUnitsMismatch", constructor String). | parseCampaignPlanManifestDocument threw raw strings via `throw manifestFieldError(...)` (26 sites) so `error.message` was undefined for any Error-based consumer; CLI sanitized detail collapsed to empty. | DEF-03 implementation defect introduced during this hardening (helper returned a code string that was thrown directly, not wrapped in new Error). | All 26 throw sites now throw `new Error(manifestFieldError(...))`; verified via compiled-core probe (isError:true, msg categorical). | Same H04 tamper case now asserts PLAN_MANIFEST_INVALID surfaces through the CLI | compiled-core probe + focused suites rerun GREEN | compatibility + canonical/isolated regressions | FIXED_BROAD_GREEN |

Status vocabulary:
- OPEN
- FIXED_FOCUSED
- FIXED_FOCUSED
- BLOCKED_LOCAL
- REFUTED_NOT_A_DEFECT

## Test-authoring corrections during hardening (not source defects)

- TA-01: B06 bounds grid used uppercase semanticScope tokens (`bounds.NONE.LOW`); the lowercase scope pattern correctly rejected them. Scopes lowercased in the test.
- TA-02: B04 asserted score-digest equality between SHA_ONLY_NO_EVIDENCE_CHANGE and NO_MOVEMENT evaluations; the digest legitimately covers component basis strings (explainability content) while both contribute 0. Assertion corrected to totals + movement contribution.
- TA-03: C06 full-reserve exploration expectation ignored perMemberCeiling (explorer consumed 4 of 5 reserved units, unused=1). Expectations corrected; allocator behavior confirmed coherent/deterministic.
- TA-04: A02 privacy scan regex matched the categorical code `PORTFOLIO_SENTINEL_REJECTED` itself (no raw value present). Scan replaced by forbidden-shape/raw-literal echo checks.
- TA-05: Floor-runner privacy scan matched safe field NAMES (`runtimeAuthorizationRequired`, `requiredAuthorizationToken`). Scanner rewritten to extract string VALUES only before applying the leak regex.
- TA-06: replanTrio passed previousProvenance into allocatePortfolioBudget, giving the contract-mutated member +20 movement relevance and selecting it. Movements now go only to classifyPlanReplan (caller-computed input), keeping the weak member unselected as intended.

## Initial hypotheses to test, not pre-confirm

- HYP-01: input-order permutation may affect tie/allocation ordering. -> REFUTED by A03/A04 permutation canons + end-to-end permutation stability across all fixtures (green pre-fix).
- HYP-02: starvation floor may interact incorrectly with hard stale/authority blockers. -> REFUTED structurally (floors iterate ranked/eligible candidates only) and pinned by all-gated fixtures + C01-C07 corpus sweep.
- HYP-03: reserve budget can over-constrain exact-fit portfolios or produce unstable unselected reasons. -> Behavior confirmed deterministic and coherent (TA-03); reasons stable (`BUDGET_EXHAUSTED_OR_RESERVE_CONSTRAINT`).
- HYP-04: zero-denominator yield metrics may produce non-finite values. -> REFUTED: null-guards verified (D02/D03) and non-finite counter inputs fail closed (D01/D04).
- HYP-05: manifest/replan identity may overreact to SHA-only source movement. -> REFUTED for scoring/reuse (B04/F-row sha-only reusable); identity ignores SHA-only movement.
- HYP-06: derivation/authority changes may not be load-bearing enough in replan invalidation. -> CONFIRMED for UNSELECTED members -> DEF-02.
- HYP-07: CLI malformed-input/error surfaces may leak arbitrary raw strings. -> CONFIRMED -> DEF-04.
- HYP-08: DEV handoff may carry enough data to be mistaken for execution authority. -> REFUTED: executable:false literal, DEV-only restriction, separate token, five runtime obligations all mechanically pinned (I-matrix); digest-only payloads.
- HYP-09: focused Phase-16A tests may miss complete-suite import/topology fallout. -> Owned by M7/M8 complete canonical + isolated regressions (pending).
- HYP-10: Node/version/topology differences may change byte determinism or CLI output. -> Owned by M7/M8 isolated checkout run (pending).
