# Phase 26 Living Plan

Task ID: phase-26-mechanical-contract-coverage-expansion
Phase: 26-MECHANICAL-RESPONSE-SEMANTIC-CONTRACT-COVERAGE
Authorization class: PHASE_26_MECHANICAL_RESPONSE_SEMANTIC_CONTRACT_COVERAGE_EXPANSION_LOCAL_SOURCE_SYNTHETIC_ONLY
Status: IN_PROGRESS
CONTINUITY_PROTOCOL_VERSION: nightwatch.agent-continuity.v2

## Purpose

Turn the large Phase 25 discovered-but-shallow source portfolio into more
useful mechanically proven response and semantic coverage while preserving
fail-closed proof, exact source provenance, privacy, deterministic identity,
and Phase 24 authority.

## Starting State

- Starting synchronized live `main` SHA: `a7ac26b6b1bb2287620b64302e60e66e468ad5c8`.
- Phase 25 is immutable terminal history. Its source substrate, no-follow
  boundary, content-aware inventory, fixed analyzers, exact joins, graph,
  invalidation, cache, operator surface, and Phase 24 bridge are reused.
- Phase 25's historical approved smoke reported 128 operations, 127 route
  proofs, 127 request contracts, 25 response contracts, 25 semantic
  contracts, 118 proven joins, 3 eligible Phase 24 surfaces, 125 exclusions,
  and 103 DISCOVERED surfaces. These are hypotheses until the fresh current
  scan reproduces them.
- Current implementation and tests outrank the stale top-level Phase 25
  documentation header. A fresh approved snapshot and current source digest
  are the baseline authority for this task.
- Permanent owner scope remains
  `FROZEN_BY_OWNER / INFRASTRUCTURE_AND_DATA_LAYER_OUT_OF_SCOPE`.

## Scope

M0–M12 below: fresh gap census; high-yield bounded response proof; semantic
contract extension only where existing vocabulary expresses the source;
cross-file joins; lifecycle/graph/drift/invalidation/cache; Phase 24 bridge;
operator intelligence; adversarial corpus; synthetic E2E; compatibility,
clean-checkout, and full-regression closure; durable documentation.

## Non-Goals

DEV/NEXT/production contact, auth reads, Alphaus writes, data/infrastructure
work, external publication, generic parsing, arbitrary code execution,
parallel authority frameworks, historical task rewrites, and CI billing
recovery.

## Safety Constraints

All sibling source access remains below `src/core/source/siblingSource.ts`.
All scan bounds and no-follow semantics remain enforced. Source text never
persists. Unknown syntax, dynamic values, incomplete branches, ambiguous
symbols, stale evidence, unproven read-only behavior, and unresolved runtime
binding fail closed. No Phase 26 analyzer may import filesystem, process,
network, persistence, browser, product, database, infrastructure, AI, or
self-development authority outside the established source-access coordinator
seam.

## Architecture / Approach

1. Verify live Git topology, read current authority records, create this task,
   and run the existing source scan to establish the current proof-gap census.
2. Inspect actual approved source patterns behind response/semantic exclusions
   and reuse the existing analyzer vocabulary and normalization contracts.
3. Add small bounded analyzer families for recurring statically provable
   response construction/reference patterns, with exact branch and symbol
   completeness requirements.
4. Derive additional semantic contracts only from proven response structure and
   existing semantic vocabulary; version analyzer identity and cache identity
   when semantics change.
5. Extend exact joins, graph lineage, lifecycle projection, shape-aware drift,
   and incremental invalidation so relevant source changes invalidate only
   dependent proof/replay/dossier state.
6. Feed descriptors directly through the existing Phase 24 bridge and extend
   gap/review operator explanations without creating a Phase 26 planner.
7. Add a data-driven adversarial corpus, privacy/determinism/cache tests, and
   a real source-to-triage synthetic campaign using production integration
   seams.
8. Certify the compatibility cone, unified gate, clean Node20 checkout,
   canonical/isolated full regressions, task continuity, and synchronized
   `main`.

## Milestones

- M0 — bootstrap, topology, authority reads, task activation, and current
  source baseline. Status: COMPLETED.
- M1 — deterministic proof-gap census and source-pattern triage. Status:
  COMPLETED.
- M2 — response analyzer architecture/versioning and highest-yield positive
  families with sound negative controls. Status: COMPLETED.
- M3 — semantic contract expansion through existing vocabulary and proof
  normalization. Status: IN_PROGRESS.
- M4 — exact cross-file response/semantic joins and currentness propagation.
  Status: NOT_STARTED.
- M5 — graph/lifecycle/drift/invalidation/cache integration. Status:
  NOT_STARTED.
- M6 — direct Phase 24 portfolio uplift and runtime/source/review correlation.
  Status: NOT_STARTED.
- M7 — operator gap intelligence and deterministic before/after reporting.
  Status: NOT_STARTED.
- M8 — broad adversarial proof-soundness, privacy, cache, and repeatability
  corpus. Status: NOT_STARTED.
- M9 — source-to-triage synthetic campaign integration. Status: NOT_STARTED.
- M10 — compatibility, hardening, gate inventory, clean Node20, and focused
  closure. Status: NOT_STARTED.
- M11 — fresh canonical and topology-correct isolated full regression with
  exact parity. Status: NOT_STARTED.
- M12 — one exact-head Actions observation if available, durable docs,
  continuity closure, final push, and synchronized main. Status: NOT_STARTED.

## Validation Strategy

Use focused source/analyzer tests after each implementation unit, then run
typecheck and hardening whenever shared analyzer/source boundaries change. At
stable checkpoints run the Phase 9–26 compatibility cone, synthetic campaign,
owner provenance, gate inventory, and project/agent checks. Terminal validation
uses the current unified local/clean gate, fresh Node20 qualification, and
canonical plus topology-correct isolated full Playwright regression. No DEV or
auth validation is part of this task.

## Decision Log

- 2026-08-25 M0: live Git (`main` and `origin/main`) is the authority; the
  Phase 25 top-level documentation drift will not be copied into Phase 26
  truth without current implementation/source evidence.
- 2026-08-25 M0: response/semantic proof expansion is the primary workstream;
  any source family that cannot be proven mechanically remains an explicit
  exclusion rather than a relaxed admission.
- 2026-08-25 M0: Phase 24 remains the sole candidate authority and all source
  access remains behind the existing confined module.
- 2026-08-25 M2: the direct-return response family is enabled only for the
  real-source surface profile. The historical semantic-discovery profile and
  analyzer identity remain stable for Phase20/21 compatibility; the real
  source cache uses a separate analyzer-set identity that includes the new
  family.

## Discoveries

The fresh approved-source baseline and bounded pattern census are recorded in
the Phase 26 STATE. The first implementation family is exact PHP direct-return
object shape because it accounts for 37 readable response-gap methods without
requiring runtime values, fuzzy joins, or a new parser architecture.

## Deferred Work

Dynamic runtime response construction, ambiguous/incomplete cross-file joins,
deployment equivalence, unresolved ownership, DEV acceptance, external CI
recovery, and owner-frozen infrastructure/data work remain deferred unless
current source proves a bounded local-only path.

## Completion Criteria

All applicable Phase 26 acceptance rows are proven with exact measured
evidence; no introduced Critical/High defect remains; response and semantic
coverage increases truthfully or remaining gaps are mechanically classified;
privacy, determinism, source currentness, Phase 24 integration, compatibility,
hardening, clean checkout, full regression parity, continuity, and project
truth pass; one exact-head Actions observation is classified truthfully if
performed; the final report is complete; and clean synchronized `main` is
left pushed at `origin/main`.
