# SPEC — nightwatch-alphaus-finding-handoff-c12-readiness-v1

## Frozen intent

Three-workstream campaign on Nightwatch base `4ca990f`:

- **A. Alphaus-compatible human-review finding handoff.** A projection-only,
  privacy-safe, evidence-backed artifact derived from the existing
  candidate/finding/dossier architecture. No filing, no messaging, no
  publication, no bounty scoring. Human review mandatory.
- **B. C-12 operator readiness.** Runbook + configuration contract +
  local-only preflight + evidence checklist + prerequisite truth. No C-12
  execution, no production contact.
- **C. Durable documentation truth.** Reconcile CURRENT_STATE, DECISIONS,
  master plan, threat model, OpenSpec, and add the Alphaus handoff context
  document with Slack-derived provenance preserved.

## Hard boundaries (frozen)

Zero production contact/reads/writes; zero DEV/NEXT execution; no Slack,
Leslie, Pondr, or external publication writes; no credential handling; no
sibling-repo writes; no force push; C-12/C-13/C-14/C-08b/C-07 not executed.

## Declared deletions

None. This campaign is additive-only unless a follow-up amendment declares
otherwise.

## Acceptance (frozen)

§39 completion criteria of the campaign brief: handoff contract + matrices,
preflight + matrices, docs reconciled, mutations all bite (target ≥16/16),
property tests green, full regression green, hardening green, clean gate
green, clean-clone green where required, truthful final report, STOP.
