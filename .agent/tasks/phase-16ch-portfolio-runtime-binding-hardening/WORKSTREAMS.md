# WORKSTREAMS — Phase 16CH

## W1 — Universe provenance / registry convergence
Harden canonical runtime-profile linkage, deterministic real-universe derivation, synthetic exclusion, recipe/currentness linkage, ordering/digest and restricted exploration state.

## W2 — Handoff / admission / authorization
Harden strict runtime-plan parsing, inert handoff semantics, manifest/handoff coherence, every categorical rejection reason, non-mutating authorization and privacy-safe failures.

## W3 — Budget mapping / work-item binding
Harden monotone-restrictive mapping across every runtime budget dimension, feasibility/reserve arithmetic, invalid numerics and exact-one selected-member-to-existing-work-item mapping.

## W4 — Identity / fingerprint / compatibility
Harden optional `portfolioBinding`, campaignId/manifestFingerprint/checkpoint identity, field-by-field tamper/drift, cross-version behavior and legacy no-binding byte/digest compatibility.

## W5 — Prepare / resume / authority ordering
Prove admission before executor-capable state, prepare zero executor callbacks, fresh resume authorization, frozen-binding equality, checkpoint drift refusal and owner-policy-before-executor.

## W6 — Launcher / external document boundary
Fuzz the single opt-in launcher pair, file/path/EOL/parser handling, sanitized errors and proof that no second runtime path or executor was introduced.

## W7 — Adversarial corpus / determinism / privacy
Build >=100 deterministic scenarios, run >=3 complete seam repetitions and drive all Phase-16CH quality floors to zero.

## W8 — Historical/full regression / closure
Run affected compatibility, canonical complete Playwright, topology-correct isolated complete Playwright, continuity/audit/project/catalog gates, exact CI truth and durable closure.

## Parallelism

Independent review/test/failure-cluster sub-agents are allowed when the harness supports them. Coding agents must use isolated worktrees/branches; no concurrent mutation of the same checkout. Parent remains the sole canonical integrator.