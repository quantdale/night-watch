# Nightwatch Executor Campaign — Read-Only Eligibility Proof Expansion + Campaign Surface Unlock

Status: ACTIVE
Planned-From: `061a0ffdd94bdaa5a7ebf526f4fbb2ea96cb434b`
Target-Branch: `main`
Campaign-Class: `IMPLEMENTATION_AND_SYSTEMIC_HARDENING`
Campaign-ID: `nightwatch-readonly-eligibility-proof-expansion-v1`

## Mission

Use a fresh current approved-source census to determine mechanically,
conservatively, and source-provably which currently excluded operations are
genuinely read-only. Feed only proven facts through the existing source,
semantic, replay, containment, owner-policy, and Phase 24 eligibility
authorities. A zero-unlock/no-admission result is valid.

The current baseline is 128 operations, 127 proven routes, 127 request
contracts, 83 response contracts, 175 semantic observations, 118 proven and
10 rejected joins, 47 mutation-capable operations, 5 independently proven
read-only operations, 3 Phase-24 eligible surfaces, 125 excluded surfaces,
and 13 rejected response-flow attempts. Recompute all of this from live local
source before implementation.

## Mandatory workstreams

1. Fresh deterministic all-surface exclusion-chain census, including sanitized
   counts, reason families, repository/language distributions, lifecycle, and
   the exact read-only/Phase-24 bottleneck.
2. End-to-end audit of source inventory, route/request/response/semantic
   contracts, mutability classification, source joins, currentness/cache,
   Phase-24 bridge/selector, replay, dossier, campaign, operator, and Control
   Center projections.
3. Bounded live-source candidate proof-family investigation. Reject HTTP verbs,
   names, comments, absence of obvious writes, inferred datastore behavior,
   fuzzy matching, and incomplete cones as authority.
4. Only if justified, implement a versioned deterministic source-bound proof
   with exact declaration/dependency identities, a mechanically complete
   write-capability vocabulary, strict budgets, invalidation, and adversarial
   falsification. Unknown/dynamic/ambiguous/stale/unsupported behavior fails
   closed.
5. Integrate additively through existing source descriptors and Phase 24. Never
   equate READ_ONLY_PROVEN with CAMPAIGN_ELIGIBLE and do not alter selection,
   diversity, replay, dossier, semantic, or owner policy without a reproduced
   correctness defect.
6. Run adversarial/privacy/determinism/performance and systemic hardening
   sweeps, then canonical, clean Node20, isolated, continuity, project, and
   terminal Git validation.

## Non-negotiable safety

LOCAL / approved read-only source / synthetic only. No DEV, NEXT, production,
authentication state, product mutation, database/datastore/cloud/
infrastructure, sibling write, publication, external coordination, runtime
AI, arbitrary filesystem/process/shell, or canonical self-development
promotion. No application-code execution. Raw source, values, credentials,
cookies, bodies, traces, arbitrary private paths, and owner-only findings stay
outside persisted/public artifacts and Git.

## Continuity

The active task directory is
`.agent/tasks/nightwatch-readonly-eligibility-proof-expansion-v1/`. Read its
SPEC.md, PLAN.md, and STATE.md in order. Update STATE.md after every milestone,
design decision, significant validation, safety event, or context boundary.
The former Control Center V2 task is terminal history and is not resumed.
