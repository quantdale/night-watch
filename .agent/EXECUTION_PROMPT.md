# EXECUTION PROMPT — W13 provider-resilient current-source unknown-defect yield

HANDOFF_PROTOCOL_VERSION: nightwatch.planner-executor-handoff.v1
Status: COMPLETE
Campaign ID: nightwatch-provider-resilient-current-yield-w13-v1
OpenSpec: openspec/changes/nightwatch-provider-resilient-current-yield-w13-v1/
Planned-From: 34517c9ba11c97407168fe5879ee03794dfff3e3
Target Branch: main
Predecessor Task ID: nightwatch-current-source-unknown-yield-w12-v1
Predecessor Status: COMPLETE

## Mission

Execute the owner-authorized W13 successor wave: close every locally closable
W12 residual first (Phase A), then freeze and execute a provider-resilient
current-source campaign across the frozen eight-repository owner-local
universe (Phase B). W11's historical arm and W12's measured evidence are
frozen; W13 must not rewrite or re-run them merely for comparability.

The campaign is complete only when Phase A is closed or truthfully blocked,
the provider-resilience policy and evaluation freeze are committed before
investigation, the fixed matrix is validly executed or categorically blocked,
every candidate reaches the existing mechanical reproduction/admission path,
metrics are derived from machine evidence, safety/leakage are proven, Group 12
is reconciled, and C-00 integration/release are complete. Zero mechanical
admissions is a valid result; a single provider's failure that the frozen
policy successfully failed over from is not a blocked wave.

## Scope

The W13 task/OpenSpec/register/evidence/report surfaces; the W12 OpenSpec
archive/sync baseline; focused Phase A fixes proven necessary by the residual
register; the provider-resilience policy and its fingerprint; the W13
evaluation freeze; current-source census and sibling identity receipts; the
ordinary bounded campaign path; existing contained reproduction/admission;
read-only post-admission novelty; Group 12 successor documentation; required
validation; and C-00 commit/integration/release.

## Ordered workstreams

1. M0 — create and validate W13 continuity/OpenSpec/task surfaces,
   archive/sync W12 into the specs baseline, route the active task, preserve
   W11/W12, and commit the activation checkpoint.
2. M1 — build and commit the Phase A residual register before fixing any
   entry; classify every entry and record whether it blocks Phase B.
3. M2 — root-cause and close the provider-failure budget mismatch (frozen
   supplemental `3` versus runtime ceiling `8`) with a regression, a
   fail-closed negative probe, and a new decision succeeding D-138.
4. M3 — re-measure the `gate:local` `SYNTHETIC_CAMPAIGN` timeout on an idle
   host and classify it as exactly one measured cause with a lane receipt;
   never raise the timeout bound.
5. M4 — extend provider failure classification to the full taxonomy with
   sanitized retention and per-transition regressions.
6. M5 — resolve current-source census truncation by safe enumeration or by a
   mechanical floor-only denominator check, with a regression.
7. M6 — guarantee aggregate measurement completeness with machine capture or
   explicit `NOT_CAPTURED` schema fields and a regression.
8. M7 — add the candidate/admission invariant negative probe and the
   failed-provider fake-progress guard.
9. M8 — reconcile Group 12 items 12.7/12.8/12.11/12.12, `PROGRAMME.json`,
   governed current-state, and README yield figures without rewriting
   predecessor history.
10. M9 — run Phase A closure validation and commit the closure checkpoint, or
    stop with an evidenced `PARTIAL — BLOCKED`.
11. M10 — author, implement, probe, and commit the provider-resilience policy
    with fingerprint, mutation detection, and fail-closed resume.
12. M11 — commit the full machine-readable W13 evaluation freeze and its
    mutation/widened-resume guards before the first investigative call.
13. M12 — execute the broad all-eight run and the eight repository-scoped runs
    in stable registry order under the frozen policy with per-provider
    attribution.
14. M13 — route every candidate through the existing mechanical
    reproduction/dossier/admission path; adjudicate novelty only after
    admission; run adversarial, leakage, sibling-write, and safety proofs.
15. M14 — mechanically aggregate all metrics, update governed documentation
    and Group 12 from evidence, and run full required validation.
16. M15 — perform the parent-programme residual analysis, inspect the diff and
    privacy surface, integrate fast-forward, verify `HEAD == origin/main`,
    release/remove the session, and record one final W13 verdict.

## Constraints

LOCAL / OWNER-LOCAL only. No DEV, NEXT, production, authenticated Alphaus
runtime, browser product journey, database/data-plane, cloud/infrastructure,
credential acquisition, or external publication. Sibling repositories are
read-only; no sibling checkout/fetch/install/mutation. Provider egress is only
through the existing configured reasoner CLI under the frozen
provider-resilience policy. No Slack/Leslie/Pondr/Notion, issue/PR creation,
force push, history rewrite, hidden-ground-truth leakage, result-driven
provider reselection, scope widening, budget tuning, timeout raising, or
weaker admission class. C-00 is mandatory.

## Validation

Focused W13 residual/policy/freeze-integrity/census/aggregation/resume/
admission tests; strict OpenSpec validation; `npm run typecheck`;
`npm run typecheck:bin`; `npm run hardening:check`; `npm run hardening:rules`;
`npm run agent:check`; `npm run handoff:check`; `npm run project:check`;
`npm run workspace:check`; `npm run session:check`;
`npm run validation:universe`; `npm run gate:local`; `npm test`; and
`npm run gate:clean` at the lifecycle-approved point.

## Acceptance / completion gates

- W11 and W12 remain unchanged; W13 has a fresh owned session and governed
  task; the W12 capability baseline is archived before the W13 delta.
- Every residual register entry resolves to `PROVEN`, `BLOCKED_EXTERNAL`, or
  `OWNER_DECISION_REQUIRED` before Phase B opens.
- Provider policy is committed and fingerprinted before probing; the first
  healthy candidate is frozen before investigation; mutation and resume guards
  are live.
- Eight-repository current census and contamination firewall are non-vacuous.
- The machine-readable evaluation freeze is committed before the first
  investigative call.
- Broad and all eight scoped runs are completed or categorically blocked under
  the frozen policy; no post-result tuning occurs.
- Candidate, reproduction, refusal, dossier/admission and novelty evidence are
  mechanically distinct and privacy-safe; failed provider calls mint nothing.
- Safety counters, sibling identity, leakage, validation, Group 12, C-00
  integration/release, and final report are internally coherent.
- The final report chooses exactly one allowed W13 verdict and never converts
  provider failure into zero yield.

## Git / reporting

Commit durable checkpoints only from the owned session. Before integration,
inspect status/diff/untracked files, declared deletions, secrets, sibling
identity, policy/freeze fingerprints, evidence coherence and unresolved
processes. If origin advances or a push is rejected, stop and reconcile
through the session CLI; never force-push. Raw campaign checkpoints stay
owner-local outside Git.
