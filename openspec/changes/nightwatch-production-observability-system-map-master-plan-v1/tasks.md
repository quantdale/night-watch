# Tasks — Nightwatch Production Observability & System Map Master Plan

## Planning campaign (this session)

- [x] M0 discover live Git state and enumerate the real sibling repository population (148 repos, 46 active)
- [x] M1 audit Nightwatch as-built from implementation and tests, not documentation
- [x] M2 reproduce the source-intelligence result with Nightwatch's own read-only census and attribute it (128 operations, 100 % `ripple-api`, 100 % YAML routes)
- [x] M3 prove the two silent ceilings (223→128 truncation at `surfaces.ts:59`; read-only proof reduces to an 11-row hand catalog at `surfaces.ts:149-162`)
- [x] M4 census the company system: backends, frontends, SDKs/contracts, deployment topology, dormant population — each conclusion evidence-classed
- [x] M5 measure the unread evidence (662 HTTP-annotated RPCs; 17 gRPC registration sites; ≈350 Requirements / ≈823 Scenarios of product OpenSpec)
- [x] M6 define the fact taxonomy and the whole-system model (`design.md §2-3`)
- [x] M7 design two-witness read-only proof (`design.md §4`)
- [x] M8 design `PROD_OBSERVE`: separate authorization class, eleven-gate chain, budgets/breakers/kill switch, five-stage ladder (`design.md §5`)
- [x] M9 design the privacy firewall and production retention model (`design.md §6`)
- [x] M10 design System Map V2 and the two coverage ledgers (`design.md §7-8`)
- [x] M11 design the bug-hunting loop and EIG prioritization (`design.md §9`)
- [x] M12 threat-model 34 hazards with prevention/detection/containment/evidence/recovery/test (`docs/design/PRODUCTION-OBSERVABILITY-THREAT-MODEL.md`)
- [x] M13 build the 16-row gap matrix and the 15-campaign dependency-ordered programme (`docs/design/PRODUCTION-OBSERVABILITY-MASTER-PLAN.md` §1-3)
- [x] M14 define the 20 hard production readiness gates (`docs/design/PRODUCTION-OBSERVABILITY-MASTER-PLAN.md` §4)
- [x] M15 write the requirement deltas (`specs/`)
- [x] M16 point durable documentation at the master plan without rewriting history
- [x] M17 verify the diff is planning-only, commit, push, confirm `HEAD == origin/main`, clean worktree, sibling repositories unmodified

## Independent second review (this session)

- [x] R0 re-measure every load-bearing claim in `audit.md` independently, read-only
- [x] R1 adversarially assess the two-witness read-only proof against real `ripple-api` source
- [x] R2 challenge the production observer identity model and the `PROD_OBSERVE` separation
- [x] R3 attempt to break the privacy firewall projection boundary
- [x] R4 review System Map V2, the coverage model and the campaign sequencing
- [x] R5 design the multi-agent/workspace isolation response (C-00)
- [x] R6 extend the threat model (T-35…T-48, R-6…R-8) and narrow `R-1`
- [x] R7 record the review in `docs/design/PRODUCTION-OBSERVABILITY-INDEPENDENT-REVIEW.md` and annotate the first explorer's artifacts without rewriting them
- [x] R8 verify planning-only diff, commit, push, confirm `HEAD == origin/main` and a clean worktree

## Future implementation campaigns — NOT AUTHORIZED

Each requires its own explicit one-shot owner authorization. Acceptance
criteria are in `docs/design/PRODUCTION-OBSERVABILITY-MASTER-PLAN.md` §2; gates in `§4`.

> **SECOND-REVIEW CORRECTIONS (R2)** — full reasoning in
> `docs/design/PRODUCTION-OBSERVABILITY-INDEPENDENT-REVIEW.md`. Corrected acceptance criteria are inline below;
> the first explorer's original wording is preserved in
> `docs/design/PRODUCTION-OBSERVABILITY-MASTER-PLAN.md` §2.

### Track 0 — Prerequisite (added by second review)

- [x] **C-00 concurrency and workspace hardening** — acceptance: per-agent `git worktree` on a session-owned branch; `agent:check` repository-hygiene invariant (no `skip-worktree`/`assume-unchanged` bits, `.git/info/exclude` matches a committed digest, `.git/hooks` samples only); file-ownership rule with a declared-deletion gate check; fast-forward-only integration protocol. **MUST precede any substantial parallel implementation.** *Implemented by `nightwatch-concurrency-workspace-hardening-c00-v1`:* `bin/workspace-integrity.mjs` + `bin/nightwatch-session.mjs` + `config/workspace-integrity.v1.json`, wired into `agent:check` and the required `WORKSPACE_INTEGRITY` gate group, with a **39-case** adversarial matrix (`tests/unit/workspaceIsolation.test.ts`, certified: exactly 39 `test('` cases, all passing) covering hazard classes A–L on disposable synthetic repositories. Two acceptance refinements are recorded rather than silently assumed: the exclude invariant compares *normalized effective patterns* against a committed policy allowlist rather than a whole-file digest (Git template text differs between clones and CI runners), and the index invariant runs per worktree and rejects `S`/`s` as well as lowercase tags (`docs/DECISIONS.md` D-102). F-32's pinned pre-C-01 baseline is `docs/design/PRE-C01-BASELINE.md`.

### Track A — Reach

- [x] **C-01 truncation truth and discovery paging** — acceptance: `ripple-api` reports 223 operations, `routeOperationsTruncated = 0`, `TRUNCATED` propagated to CLI + contract + ledger. **Second review adds:** (a) a permanent **no-eviction regression assertion** — pre-change operation identities must be a subset of post-change identities, because the counter is global and `surfaces.ts:832` sorts `repoId` first, so any new `alphauslabs/blueapi` operation would otherwise silently evict all of `ripple-api`; (b) **enumeration** truncation is in scope, not only operation truncation — `TRUNCATED` must carry `{limit, examined, dropped}` and `dropped` must be **computed** (today the walk aborts at `siblingSource.ts:273` and records a one-shot flag, leaving ~2,000 `ouchan` files invisible). *Implemented and certified by `nightwatch-truncation-truth-discovery-paging-c01-v1` at implementation anchor `4d8c88aba9bbb53b900e9f1d2c24bc3ed7b95778`:* the silent `MAX_DISCOVERED_OPERATIONS = 128` cap is replaced by `MAX_PROJECTED_OPERATIONS = 4096` with per-repository round-robin fair dealing; `ripple-api` now reports **223 operations with `routeOperationsTruncated = 0`**; the permanent no-eviction regression is `tests/unit/sourceOperationCompleteness.test.ts`; enumeration truncation is separated from content-read exhaustion by the disjoint `enumerationBudgetRejections` / `contentBudgetRejections` counters and the new `SourceInventoryCompleteness` contract, and `dropped` is **computed** per repository (an aborted walk reports `null` totals with `remainingUnknown: true` rather than a fabricated zero); completeness is propagated through the eligibility census (`v2 → v3`), the read-only census (`v1 → v2`), every `nightwatch-intelligence` source command, and the Control Center. `gate:local` and `gate:clean` PASS on all eleven required groups; full regression 2,753 passed / 13 skipped / 0 failed. The response-contract metric is reconciled in `docs/DECISIONS.md` D-105 (`83` and `43` are one metric at two analyzer versions, both measured over the 128 cap; the first whole-population figure is **58 of 223**).
- [x] **C-02a OpenAPI admission** *(added by second review; precedes C-02b)* — **dependency (`F-29` resolved):** requires **C-01 only**; C-05 is **not** a predecessor. C-02a admits `blueapi/openapiv2` — a new ROOT inside the already-admitted `alphauslabs/blueapi` repository (`APPROVED_ROOTS['alphauslabs/blueapi']` is `['billing']` today) — under its own explicit owner authorization. `blueinternal/openapiv2` is **out of C-02a scope and blocked behind C-05**, because `alphauslabs/blueinternal` is not a member of the repository universe at all. Acceptance: `blueapi/openapiv2` admitted to `APPROVED_ROOTS`; **zero new parsers**; ≥ 591 `blueapi` operations with verb, path and operationId through the existing `parseOpenApiRoutes`; `$ref` → `definitions` binding yields ≥ 400 response contracts from the 1,179 available definitions; the artifact is classed `SOURCE_FACT (GENERATED_ARTIFACT)`, carries a generation-currency check against the proto surface, and is barred from being the sole basis of a production admission. The ~57 `blueinternal` operations are **not** part of C-02a's acceptance; they are claimed by C-05. **DONE:** `blueapi/openapiv2` admitted (`APPROVED_ROOTS['alphauslabs/blueapi'] = ['billing', 'openapiv2']`); zero new parsers — the existing `parseOpenApiRoutes` gained in-document `$ref` → `definitions` resolution only; **591** blueapi operations with verb, path and a distinct `operationId`, taking the whole population from 223 to **814** (223 Ripple + 591 blueapi, `droppedOperations: 0` in both, no deduplication); **970** response contracts bound from the 1,179 available definitions with **0** unresolved (591 `default` + 379 `200`), and 591 operations reaching `responseProof: PROVEN`; evidence classed `SOURCE_FACT (GENERATED_ARTIFACT)` with a proto-surface generation-currency check that reports `UNKNOWN` until C-02b supplies a corroborator, and a deny-only production-admission gate (`GENERATED_ARTIFACT_SOLE_EVIDENCE`) with no `GRANTED` member by construction; all 223 pre-C-02a operation identities preserved; see `docs/DECISIONS.md` D-106.
- [x] **C-02b protobuf source intelligence** *(was C-02)* — acceptance: ≥ 147 operations from `blueapi/billing` with verb, path, request and response message; zero operations from commented or malformed options. **Second review:** now required only for streaming RPCs (90 repo-wide), the proto service ↔ RPC symbol that C-03 joins on, and corroboration of the generated OpenAPI artifact — not as the primary route to the RPC surface. **DONE** by `nightwatch-protobuf-source-intelligence-c02b-v1`: **147** proven RPCs measured at `blueapi@691422e5`, each carrying verb, path, request and response message; **zero** facts derived from comments, strings or malformed options (51/51 lexer cases, including 5 comment and 3 string cases). Streaming is measured rather than estimated — **114 unary / 0 client-streaming / 33 server-streaming / 0 bidirectional** — which REFUTES the '90 streaming RPCs repo-wide' figure this row previously carried; the historical number and its cause are preserved in the campaign REPORT rather than quietly replaced. Per-operation OpenAPI corroboration reports seven distinct outcomes, each with a fixture built to produce it, and currency is upgraded only on identity evidence (counts-agree-content-differs fixtures stay non-CURRENT). C-01 no-eviction holds over the real before/after populations, with `ripple-api` keeping 223. Certified at exact-head CI run `33680339948` / job `100415095920` at `321f11b`, all eleven required groups PASS on Node 20 with receipt `receipt:sha256:1307a41faa4f1e2812939d15`; `gate:local` PASS `receipt:sha256:372e981fa39b01b87b8581da` and `gate:clean` PASS with inner receipt `receipt:sha256:d3455222554c6eff68374d48` and `siblingWrites: 0`; canonical regression 3,228 / 3,215 / 13 skipped / 0 failed; 86 C-02b tests, all gate-registered; 15/15 negative probes detected and restored; DEF-C02B-1 and DEF-C02B-2 introduced, both repaired and reported.
- [x] **C-03 Go/gRPC topology binding** — acceptance: ≥ 12 ouchan services bound to ≥ 12 proto services as `SOURCE_FACT`. **DONE** by `nightwatch-go-grpc-topology-binding-c03-v1`: **12** proto services bound to their ouchan registrations as `SOURCE_FACT`, each with provenance and an embedding corroboration; a truncated enumeration never becomes a completeness claim. Certified at exact-head CI run `33689899601` / job `100445996051` at `0d86b6d`, all eleven required groups PASS on Node 20 with receipt `receipt:sha256:b8765cd35533224fa4f8090e`; `gate:clean` PASS with inner receipt `receipt:sha256:7d05be06a2eaa16eb8cf6163` and `siblingWrites: 0`; canonical regression 3,288 / 3,275 / 13 skipped / 0 failed; 19/19 negative probes detected and restored; DEF-C03-1 through DEF-C03-3 were introduced by the campaign, and all three are recorded as found, repaired and reported rather than omitted. Certification suites `tests/unit/c03GoRegistration.test.ts` and `tests/unit/c03GrpcTopology.test.ts` are gate-registered (R-12 moved that guarantee into `config/campaign-certification.v1.json`).
- [x] **C-04 frontend consumer intelligence** — acceptance: ≥ 400 frontend→route edges; no `SOURCE_FACT` edge from a non-literal path. **DONE** by `nightwatch-frontend-consumer-intelligence-c04-v1`, with ONE ACCEPTANCE FIGURE NOT MET and deliberately not rounded up: the campaign yields **382** classified consumer edges, so the ≥ 400 criterion **FAILS at 382**. The cause is measured rather than assumed — the frontend universe is a single admitted repository whose enumeration completes (it is not truncated), and reaching 400 requires admitting another, which is C-05's work. The safety criterion holds exactly: **zero** `SOURCE_FACT` edges from a non-literal path, with **164** edges joined to a specific backend operation. Certified at exact-head CI job `100614907944` at `0323d5f`, all eleven required groups PASS on Node 20 with receipt `receipt:sha256:6cba46d8990802d39f4b4e26`; `gate:clean` PASS with inner receipt `receipt:sha256:9ad8e572fe6eed991f7a7dea` and `siblingWrites: 0`; canonical regression 3,338 / 3,325 / 13 skipped / 0 failed; 16/16 negative probes detected and restored; DEF-C04-1 and DEF-C04-2 introduced, both repaired and reported. The residual edge-count shortfall is carried by C-05, not closed here.
- [ ] C-05 universe discovery and admission hygiene — **dependency (`F-29` resolved):** no upstream dependency; **hard predecessor of every new REPOSITORY admission** — `alphauslabs/blueinternal`, `wave-api`, and therefore the `≥ 900` operations metric. C-05 does **not** gate C-02a's `blueapi/openapiv2` root, which is a per-root change inside an already-admitted repository. Acceptance: one owner-approved allowlist; no persisted mutable git state; unapproved repositories provably unscanned. **Second review adds:** owner-gated **admission-set expansion** with named target roots (`alphauslabs/blueinternal` incl. `openapiv2`, `wave-api`) — no other campaign can deliver the plan's `≥ 900` operations metric.

### Track B — Proof

- [x] **C-06 two-witness read-only proof (PHP lane)** — acceptance **replaced by second review**: (a) **zero false positives** on a negative corpus that must include the two historical D-79 admissions, a GET whose resolved closure reaches a write, **a GET whose middleware performs an external call** (`MarketplaceSubscriptionMiddleware`), an unclassified callee, a depth-bound overflow, a dynamic-dispatch callee and an ambiguous join; (b) **100 %** callee-identifier classification coverage for every admitted repository; (c) every proof **kind-diverse and effect-mandatory** — ≥ 1 declaration witness AND ≥ 1 effect witness, with `W-SPEC` barred from production admission; (d) each proof carries its join precondition, its repository **inventory-completeness** assertion and its vocabulary digest; (e) the effect closure is rooted at the **resolved middleware pipeline plus handler**; (f) the 11-row catalog is no longer an input. The achieved count is **reported, never gated** — `≥ 200` is withdrawn as a pass/fail criterion. *Implemented (PHP lane) by `nightwatch-php-readonly-proof-c06-v1`:* `src/core/source/effectVocabulary.ts` + `phpPipeline.ts` + `phpEffectClosure.ts` + `readOnlyProof.ts`, with classification moved to the post-join proof stage of `discoverSourceSurfaces` and the surface descriptor advanced `v4 → v5` to carry the proof. The eleven-row catalog no longer classifies; a **38-case** corpus (`tests/unit/c06PhpReadOnlyProof.test.ts`) covers all eight required negative cases with zero false positives plus a positive non-triviality case. **Measured, not targeted:** `READ_ONLY_PROVEN` over the approved universe fell **5 → 0** (814 operations, 0 truncated); 79 `ripple-api` GET routes are now positively shown to perform an outbound call through their resolved pipeline, and 6,114 unclassified callee identities block promotion via `CALLEE_CLASSIFICATION_INCOMPLETE`. **Deferred by scope:** `W-EFFECT_RPC` (C-03), `W-DECLARED_VERB` (C-02b) and `W-SPEC` (C-09) report `UNSUPPORTED`, so 100 % callee coverage for a Go repository and the RPC effect witness remain open with their owning campaigns.
- [ ] C-07 derived endpoint semantics and generated targets — acceptance: registry fully derived; ≥ 30 generated DEV targets; ≥ 1 product finding on DEV
- [ ] C-08 deployment-fact binding — acceptance: every operation carries a binding class; U-1 recorded as an explicit unknown, never inferred
- [ ] **C-08b `mochi` read-only manifest access** *(added by second review; start immediately — organizational lead time)* — acceptance: read-only access to `mochi`'s `services/{env}/{appproxy,serviceproxy}/ingress.yaml`; `U-1` and `U-2` settled; a `DEPLOYMENT_FACT` route → endpoint binding exists for every route proposed for production admission. **Precondition for C-13**: without it P2 grants authority from an `INFERENCE`, violating `I-3`.
- [ ] C-09 spec-derived expectations — acceptance: ≥ 40 admitted expectations bound to `repo@SHA:path`; non-checkable scenarios grant nothing

### Track C — Production

- [x] **C-10 privacy firewall** — acceptance: all five `design.md §6.5` tests green; projection cone import-isolated; persistence audit clean. **DONE** by `nightwatch-production-privacy-firewall-c10-v1`: `src/core/prodPrivacy/**` (pure, import-isolated) and `src/core/prodEvidence/**` (persistence) deliver the allowlisted structural projection, an independent persistence firewall, the `$HOME/.nightwatch/prod-findings/` root and the Control Center exclusion. **F-14 resolved** — a key literal crosses only as a proven member of a source-proven finite key vocabulary; an unproven key yields bounded cardinality and value structure only, never the literal and never a digest of it. **F-15 resolved** — `prodstruct:sha256:` is value-free, unsalted and cross-campaign comparable, and NO durable value digest exists; `§6.2`/`§6.4`'s per-campaign salt is SUPERSEDED (D-113). **F-16** privacy model only (opaque handles; no request execution path), with route identity a source-proven vocabulary MEMBERSHIP after DEF-C10-5 showed shape-only validation accepted concrete customer identifiers. **F-17** ephemeral profiles with crash-path cleanup. **F-18** resolved by resolved-path equivalence on every construction route including the test seam. FINAL C-10 CERTIFICATION (post-DEF-C10-5, verified read-only against the GitHub API during C-10.5): 93 C-10 tests; canonical regression 2,932 total / 2,919 passed / 13 skipped / 0 failed; synthetic campaign 221 / 221; exact-head CI run `33601265465` / job `100155266632` at `cb631cc4af3c3572f4cbf78da04a8265075fbfa5` on Node 20 with receipt `receipt:sha256:f38b272bec3a37464257e194`, all eleven required groups PASS; local and clean Node 20 gates green at `23523cc`. Earlier intermediate checkpoints of this row recorded 81 C-10 tests, a 2,920 / 2,907 / 13 / 0 regression and a 128→209 synthetic campaign; those are HISTORICAL values from before the DEF-C10-5 repair and are retained here as such, not as current certification. C-10 does NOT authorize production observation. See `docs/DECISIONS.md` D-112, D-113, D-114.
- [x] **C-11 `PROD_OBSERVE` safety kernel** — acceptance: PQ receipt with all eleven gates exercised against mock production at 100 % denial for non-admitted cases; **zero real contact**; D-4 textually intact. **DONE** by `nightwatch-prod-observe-safety-kernel-c11-v1` at substantive anchor `787966061beb91de0002fd114ca04e488b44be48`: the kernel keeps production unreachable unless every authority grants it, the production decision path stays separated from the DEV/NEXT one, and **zero real production contact** occurred — the eleven gates were exercised against mock production only. Certified at exact-head CI run `33665872548` / job `100367351818` at `150dfcc`, all eleven required groups PASS on Node 20, `environmentClass: CI`, receipt `receipt:sha256:1d991b9a10d4cad618c0f533`; `gate:local` PASS `receipt:sha256:2ff143e71ea8974847053723` and `gate:clean` PASS with inner receipt `receipt:sha256:997ebf6461843448173e889d` and `siblingWrites: 0`, both at `7879660`; canonical regression 3,141 / 3,128 / 13 skipped / 0 failed with **0** new skips. C-11 grants NO production observation authority: C-12 remains a separate campaign requiring new explicit owner authorization. Certification suites `tests/unit/c11ProdObserveKernel.test.ts` and `tests/unit/c11ProdObserveEvidence.test.ts` are gate-registered.
- [ ] C-12 P1 passive production observation — acceptance: ≥ 3 sessions, zero requests issued by Nightwatch, zero raw values persisted, baselines recorded
- [ ] C-13 P2 bounded active production reads — acceptance: ≥ 5 campaigns, zero safety/privacy events, zero budget overruns, every request traceable to a two-witness surface
- [ ] C-14 P3 bounded production replay — acceptance: ≥ 1 candidate reproduced with exact fingerprint equality; dossier privacy-clean

### Track D — Visibility

- [ ] C-15 System Map V2 and coverage ledgers *(C-15a **DONE**, C-15b **DONE**, C-15c OPEN — the HTTP transport)* — **split by second review**: **C-15a truncation and ledger surfacing ships with C-01** — *shipped with C-01 at `4d8c88a`: the Control Center source-summary contract advanced `v2 → v3` with a `completeness` block carrying `{state, coverageState, limit, total, examined, projected, dropped, truncated, remainingUnknown}` plus disjoint `enumeration` and `contentRead` sub-blocks, and the UI marks a truncated or unknown population visibly; the seven-state R2 coverage vocabulary (`PROVEN, UNPROVEN, UNSUPPORTED, TRUNCATED, STALE, UNKNOWN, UNMEASURED`) is implemented in `src/core/source/completeness.ts` and may deny authority but never grants it. **C-15b graph rebuild is now DONE; C-15c, the HTTP transport, is the open half.*** *C-15b delivered by `nightwatch-system-map-v2-c15b-v1`:* source-graph contract v2 with exactly one fact category per node and per edge, the evidence-status vocabulary, `ProjectionBound` carrying `{limit, total, projected, dropped, truncated, remainingUnknown}`, progressive disclosure L1-L4 each independently bounded and each reporting its exact drops, a deterministic content-addressed layout whose identity binds the graph digest, engine id, engine version, options and projection version while excluding timing, all eight operator queries individually tested, and a renderer that pans, zooms, searches, filters and drills down — replacing the measured **24-node / 48-edge** ceiling that two `slice` calls and a modulo-3 grid had imposed against a 1,000/2,000 contract. Control Center authority and the C-10 production-store exclusion were re-proven rather than inherited. Certified at exact-head CI run `33750522362` / job `100632776636` at `c770721`, all eleven required groups PASS on Node 20 with receipt `receipt:sha256:2f18e3765638cb523b58aeea`; `gate:clean` PASS with `siblingWrites: 0`; canonical regression 3,396 / 3,383 / 13 skipped / 0 failed; 20/20 negative probes detected and restored; DEF-C15B-1 and DEF-C15B-2 introduced, both repaired and reported. **NOT delivered by C-15b, and stated rather than implied finished:** the L1-L4 projections and the server-side layout are proven in `src/core/systemMap/**` but are NOT exposed over the Control Center HTTP API, and the UI still consumes the v1 source-graph path. That closure is C-15c's, so this row stays open until C-15c lands. Original wording: (the only way an operator can see eviction and enumeration loss); **C-15b graph rebuild** follows C-03/C-04. Acceptance: layout computed **server-side** and content-addressed, with the pinned ELK version and option set inside the layout digest; the 10⁴-node budget is replaced by "no projection exceeds its contract bound, every bound reports `{limit, total, dropped}`, and the interactive frame budget is met at the largest permitted projection (1,000/2,000)"; all eight operator queries answerable; `TRUNCATED` rendered (both graph contracts already compute it — the UI ignores it); the coverage ledger uses **seven** buckets `{proven, unproven, unsupported, truncated, stale, unknown, unmeasured}`; Control Center still GET/HEAD-only with `executionAuthority: NONE`.
- [x] **G-16 and EIG owners** *(added by second review)* — `G-16` (one derived figure source) and EIG prioritization (`design.md §9.2`) appear in the gap matrix and the design but are owned by no campaign. Assign both. **DONE by `nightwatch-eig-prioritization-c16-v1`: BOTH ASSIGNED TO C-16.** The two are unrelated except in being orphaned, and this row's phrasing invites reading them as one prioritisation concern — which would have left the documentation-truth requirement unimplemented while an EIG module got built. They are recorded separately below.
  - **`G-16` → C-16, implemented.** Its authoritative definition is "stale duplicate figures in durable docs", fixed by "one derived figure source", asserted as "no document contains a census figure absent from the current ledger". `src/core/source/censusFigureLedger.ts` is that single source: `CENSUS_FIGURES` declares each measure and its current value with the campaign that established it, and `checkCensusFigures` refuses any tagged figure the ledger does not support. A superseded narrative is RETIRED by one explicit `<!--census:historical-->` marker rather than deleted. The figure vocabulary is an explicit tag rather than free-text number scanning, for the same reason C-08 refused to infer a deployment fact from a hostname: guessing which numbers in a document are census figures yields both false positives and false negatives, and neither is acceptable in a truth check. A first draft exempted any line containing `was `, which would have silently exempted a large fraction of English prose — an exemption that fires by accident is worse than none, because the check then passes while proving nothing.
  - **EIG → C-16, implemented.** `src/core/source/expectedInformationGain.ts` keeps §9.2's SHAPE and refuses its arithmetic: every factor is a bounded integer level with named members, the score is an exact rational held as numerator and denominator and never divided, and ordering compares `a/b` against `c/d` as `a·d` against `c·b` in integers — so the order never depends on rounding. Each factor has an explicit `UNKNOWN` level sitting mid-scale, never 0 and never maximum, because under a multiplicative form a zero DELETES a target and a maximum PROMOTES one, and "we do not know" is neither. Ties break by target id, giving a total order, so two runs cannot disagree. `change_recency` consumes proven source-change evidence and no level names a duration, per the prohibition on wall-clock age. §9.2's explicit non-goal is asserted directly: a broad shallow sweep scores worse than a deep pass over new contracts. **EIG grants nothing** — the projection carries `grantsAuthority: false` as data, no authority surface imports the module, and it takes no admission, environment or credential input.

### Designed but not planned

- [ ] P4 autonomous read-only production campaigns — blocked on U-3 (organizationally enforced read-only observer identity), an owner/organization decision outside Nightwatch's scope
