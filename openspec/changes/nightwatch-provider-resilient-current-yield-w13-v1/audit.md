# Audit — W13 provider-resilient current-source unknown-defect yield

Audited at live starting SHA `34517c9ba11c97407168fe5879ee03794dfff3e3`
before any W13 provider probe or investigative call.

## Predecessor and authorization

W12 (`nightwatch-current-source-unknown-yield-w12-v1`) is COMPLETE with
verdict `PARTIAL — BLOCKED`: one valid broad run reached all eight
repositories and produced 2 candidates, 2 `MISSING_REPRODUCTION` refusals, and
0 admissions, while all eight repository-scoped runs were provider-blocked
before source actions once the single frozen provider
(`opencode-go/glm-5.3`) degraded with 23 runtime timeouts and 33 nonzero exits.
W11 (`nightwatch-autonomous-yield-proof-w11-v1`) remains the earlier frozen
predecessor (`PARTIAL — BLOCKED`, historical EXACT `0/13`, invalid
provider-blocked unknown arm). The owner-authorized W13 prompt and the owner
instruction to apply the pending OpenSpec change authorize this successor
wave: Phase A residual closure, a predeclared deterministic provider-failover
policy, a re-frozen broad-plus-eight-scoped matrix, mechanical yield
measurement, and C-00 integration. Neither predecessor arm is re-run.

## Live Git and workspace

Canonical `main` and `origin/main` both resolve to
`34517c9ba11c97407168fe5879ee03794dfff3e3` at activation. W13 is owned by C-00
in the session worktree on branch
`session/nightwatch-provider-resilient-cu-623c6535`, based at the same SHA.
Four pre-existing stale worktrees remain owner-attention records; W13 does not
adopt, release, remove, or modify them.

## Existing capability boundary

W13 composes the existing approved eight-repository source universe, source
currentness checks, diverse source index, bounded reasoner loop via
`bin/nightwatch-agent.mjs campaign run` and
`src/core/agentRuntime/localCampaign.ts`, contained reproduction providers,
dossier/admission gates, leakage/redaction controls, and owner-local
persistence. Phase A may add focused classification, measurement-completeness,
and fail-closed integrity checks to those existing surfaces. No new network,
shell, product, database, credential, sibling-write, or publication authority
is granted by this change.

## Required execution audit

Before the first Phase B probe, the task MUST commit the provider-resilience
policy and its fingerprint. Before the first investigative call, it MUST
commit the W13 machine-readable evaluation freeze. During and after
execution, the report MUST cite exact provider/run receipts, census and
identity snapshots, policy and freeze fingerprints, mutation/replay/resume
probes, candidate/admission/refusal proofs, novelty receipts, safety counters,
validation receipts, and C-00 integration evidence. Provider-blocked or
incomplete evidence MUST remain categorical and MUST NOT be published as zero
yield. Phase B MUST NOT open while a residual register entry is
`LOCAL_FIX_REQUIRED` or otherwise blocks the wave by its own classification.
