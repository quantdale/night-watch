Implementation was originally declared outside the planning-only audit campaign that created this change. The umbrella campaign `nightwatch-priority-audit-remediation-sequence-v1` (M4, owner-authorized) later performed the implementation; each item below records implementation evidence from the live checkpoint, distinguishing planning completion from implementation completion. The original planning-only STATE remains readable as planning history.

## 1. Establish the complete boundary

- [x] 1.1 Syntax-aware discovery of every writer reachable under authenticated run roots: `bin/lib/authenticatedWriterCensus.mjs` (+ `.d.mts`) resolves recorder-dir handles, artifacts-tree joins (incl. one-level const indirection), resolved file-parameter publishers, artifacts-root-declared removals, and locally-defined wrapper writers. Live census: 22 writers / 8 production / 9 `tests/manual/` bypass writers (phase2a/2a-canary/2b/2c/4/5/9b/10b/22 + destinationManifest + retention + auth directRunner), digest-bound, floors in the hardening rule.
- [x] 1.2 Regressions added in `tests/unit/authenticatedEvidenceMinimization.test.ts`: lowercase identifier leakage (`acme1234`, `accountabc`, `inv202506`, encoded `%61cme1234`), constructor/manifest sanitization, repository-snapshot firewall, summary notes/hard-failure categorical forms, auxiliary-writer unknown/stale/duplicate failures, and the late authenticated-mode transition (hardening + ambiguity refusal + irreversibility).
- [x] 1.3 Schemas defined and declared: route template (`nightwatch.proven-route-table` v1, schema-declared), writer profiles (`WRITER_CLASSES` + per-root capability contracts), publication shape (wx/0600 temp → fsync → atomic rename → dir fsync → post-verify; target-checked appends), categorical diagnostics (`RUN_EVIDENCE_FIREWALL_*`, `RUN_EVIDENCE_PUBLISH_TARGET_UNSAFE`, `AUTHENTICATED_EVIDENCE_TRANSITION_*`, `AUTHENTICATED_MANIFEST_KEY_UNSAFE`, `<UNKNOWN_ROUTE>`, `<RULE:id>`).

## 2. Implement provenance-bound minimization

- [x] 2.1 Path heuristics replaced: `redactAuthenticatedUrl` persists `origin + proven template` or the categorical unknown-route marker only; the lexical `looksLikeIdentifier`/`safeRouteWord` guessing is removed and hardening forbids its return (HC-127 pins the no-match branch). Userinfo/query/fragment never persist; unparseable/relative inputs reduce to the marker; binding is opt-in from explicit endpoint authority (`provenRouteTableFromRules`, first-match in bind order).
- [x] 2.2 One final typed persistence firewall for every authenticated artifact: closed kind vocabulary (`manifest|events|proxy|repositories|summary`) with per-kind byte budgets pinned to the run-evidence READER limits, NUL refusal, structural private-shape screen (NW-AUD-019 v2 primary) + text screen defense-in-depth, called immediately before every publication (rewrite chokepoint `this.firewall(kind, value, serialized)` and the event hot path `this.firewall('events', ev, line)`); free-string/raw persistence inputs eliminated (notes collapse to `RUN_NOTE_PRESENT`, hard-failure messages/reasons categorical through the SHARED `KNOWN_RUN_FAILURE_REASONS` vocabulary, event text suppressed by the sanitizer, unknown firewall kinds fail closed).
- [x] 2.3 Converted inputs: repository facts sanitize + firewall in authenticated mode (hostile branch-name regression refuses before the file exists); summaries categorical; notes tokenized; proxy records sanitized before `proxy.jsonl` publication and summarized from the sanitized stream. Semantic auxiliary writers (phase9b/10b/22) are registered under the closed `MANUAL_PUBLISHER` profile; their recorder-mediated output crosses the recorder firewall, and their direct owner-local files never enter run-evidence readers.

## 3. Harden publication and transitions

- [x] 3.1 Authenticated mode is established at construction where known (constructor manifest crosses the sanitizer, dir 0700, artifacts 0600) and a late transition is an irreversible identity-checked hardening transaction: two-phase verify-then-tighten (symlink/foreign-owner/non-file/oversize refuse with the flag untouched), executed BEFORE the flag flips (HC-126), idempotently re-tightening on repeat so permissions can never regress.
- [x] 3.2 Publication through `publishJson`: collision-resistant hidden temp (`wx`/0600, 16-byte randomness), file fsync, atomic rename, directory fsync, post-publish owner/regular verification; appends target-checked (`assertPublishTarget`) then owner-only. Orphaned recorder temporaries are inert (the reader admits only known names; whole-run retention removes run directories conservatively as units).
- [x] 3.3 Total hardening registry: `AUTHENTICATED_WRITER_REGISTRY` with `UNKNOWN_WRITER` / `STALE_REGISTRY` / `DUPLICATE_REGISTRY` / `EMPTY_CENSUS` fail-closed codes, longest-prefix claim resolution, non-zero floors and digest in `checkAuthenticatedEvidenceFirewall`; registry drift is mutation-proven (HC-128).

## 4. Adversarial and mutation proof

- [x] 4.1 Ordinary-looking IDs (lowercase, mixed-case, numeric, encoded/percent-decoded), all URL positions (userinfo, query, fragment, root, relative/unparseable, non-HTTP schemes), nested/compound sensitive keys under event data (dropped by the shared structural authority with numeric cost-class values retained), raw notes/snapshots/errors (categorical or refused), and writer profiles (census classes asserted by rule + synthetic unknown/stale/duplicate negatives) are exercised with synthetic values only.
- [ ] ~~4.2 Test unsafe roots/leaves, late transitions, collisions, concurrent recorders, killed writes, partial generations, and reader compatibility.~~ [PARTIAL: unsafe roots (symlinked manifest and planted symlink under the run dir), late transitions (ambiguity refusal with flag untouched, irreversibility, idempotent re-tighten), size/permission bounds, and reader compatibility (`controlCenterRunEvidenceReader` + authenticated smoke PASS against the new writer) are proven. NOT yet exercised explicitly: killed-mid-write partial generations, concurrent recorders on one directory, and temp-name collision under adversarial pre-seeding — recorded rather than claimed.]
- [x] 4.3 Registered mutations, all DETECTED (probe campaign `rules=1 probes=9 detected=9 undetected=0`): heuristic preservation ⇒ HC-127; missing template proof ⇒ HC-127 (no-match branch) + proven-route unit refusals; sanitizer bypass ⇒ HC-125 (shared key authority) and HC-111..HC-113 upstream; direct writes ⇒ HC-121 (rewrite firewall) + HC-122 (append target check) with the single-chokepoint rule assertions; permissive manifest keys ⇒ HC-129; unsafe mode transition ⇒ HC-126 (harden-before-flip) with the ordering assertion; permission/publication checks ⇒ HC-121/HC-122 + wx/fsync assertions; registry drift ⇒ HC-128. Legacy list items mapped, none faked.

## 5. Acceptance and handoff

- [x] 5.1 Focused suites PASS — authenticatedEvidenceMinimization (16), authenticatedWriterCensus (5), evidence, redaction, controlCenterRunEvidenceReader, authenticated/passive/safety/negative/proxy smokes, journeyEngine, documentLifecycle, bootstrapHooks, privateStructuralScreening, privateConsumerCensus, selfDevAdoptionCli, evidenceRetention, privateArtifactAtomic, destinationManifest, phase17EvidenceHardening: 190/190 in the checkpoint battery; `npm run typecheck` PASS; `checkAuthenticatedEvidenceFirewall` + `checkPrivateSurface` PASS with probe campaigns 9/9 and 11/11 DETECTED; `npm run schema:check` PASS. Checkpoint gates (`gate:dev`, `gate:milestone`) run for this commit — receipts recorded in the umbrella STATE validation ledger under the M4 entry; full regression / clean / topology gates belong to umbrella milestone M6 final certification and are not claimed here.
- [x] 5.2 docs/SAFETY_MODEL.md and docs/ARCHITECTURE.md audited for staleness against the new route/transition/firewall behavior — no contradicting prose found (no document describes the removed lexical heuristic); this change's design/tasks carry the behavior documentation; strict `openspec validate nightwatch-authenticated-evidence-minimization-integrity-v1 --strict` PASS; privacy/diff inspected; synthetic values only, no real authenticated execution, no external publication. C-00 integration is owned by the umbrella campaign (M6) through the single session worktree, not by this planning change's original closure.
