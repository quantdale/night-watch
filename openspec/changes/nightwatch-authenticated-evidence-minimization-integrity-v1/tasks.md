Implementation is explicitly outside the planning-only audit campaign that created this change. These tasks are declared not in scope for the current task; none has been performed.

## 1. Establish the complete boundary

- [ ] ~~1.1 In a separately authorized implementation session, syntax-discover every recorder writer and direct write reachable under authenticated run roots.~~
- [ ] ~~1.2 Add regressions for lowercase identifier leakage, raw constructor/manifest values, repository snapshots, summary notes, auxiliary writers, and late authenticated-mode transition.~~
- [ ] ~~1.3 Define route-template, safe DTO, writer profile, publication, and categorical diagnostic schemas.~~

## 2. Implement provenance-bound minimization

- [ ] ~~2.1 Replace path heuristics with exact endpoint/template provenance and conservative unknown-route reduction.~~
- [ ] ~~2.2 Implement one recursive closed-schema firewall for every authenticated artifact and eliminate free-string/raw-object persistence inputs.~~
- [ ] ~~2.3 Convert repository facts, summaries, notes, proxy records, and semantic auxiliaries to bounded safe DTOs.~~

## 3. Harden publication and transitions

- [ ] ~~3.1 Require authenticated mode at construction where known and implement an irreversible identity-checked hardening transition for remaining callers.~~
- [ ] ~~3.2 Publish through owner-only, symlink-safe, bounded, collision-safe, crash-consistent generations with conservative recovery.~~
- [ ] ~~3.3 Add a total hardening registry for writers and reject unknown/stale/bypass entries.~~

## 4. Adversarial and mutation proof

- [ ] ~~4.1 Exercise ordinary-looking IDs, encodings, Unicode, nested keys, all URL positions, raw notes/snapshots/errors, and every writer profile.~~
- [ ] ~~4.2 Test unsafe roots/leaves, late transitions, collisions, concurrent recorders, killed writes, partial generations, and reader compatibility.~~
- [ ] ~~4.3 Register mutations for heuristic preservation, missing template proof, sanitizer bypass, direct writes, permissive DTO keys, unsafe mode transition, and permission/publication checks.~~

## 5. Acceptance and handoff

- [ ] ~~5.1 Run focused redaction/evidence/browser/auth/Control Center suites, typechecks, privacy/hardening mutations, continuity/workspace/project checks, local/clean/topology gates, and full regression.~~
- [ ] ~~5.2 Update privacy/safety/architecture documentation, strict-validate this change, inspect privacy/diff, and integrate only through an owned C-00 session without authenticated or external execution.~~
