Implementation is explicitly outside the planning-only audit campaign that created this change. These tasks are declared not in scope for the current task; none has been performed.

## 1. Establish the egress authority denominator

- [ ] ~~1.1 In a separately authorized implementation session, syntax-discover every browser route/CDP/WebSocket continuation, redirect path, API relay, proxy tunnel, endpoint registry producer, and journey intent/settlement consumer.~~
- [ ] ~~1.2 Add regressions showing an unclassified initialization request is continued, a request delayed beyond 250 ms loses action causality, and a same-host redirect follow-up is governed only by host policy.~~
- [ ] ~~1.3 Define immutable admission-handle, initialization-exemption, generation, currentness, and categorical refusal schemas with privacy-safe identities.~~

## 2. Implement pre-effect semantic admission

- [ ] ~~2.1 Require exact source/provenance/currentness-bound `KNOWN_READ` proof before any product API request can continue.~~
- [ ] ~~2.2 Replace passive unknown with finite mechanically proven initialization exemptions; refuse unknown, stale, ambiguous, method/route drift, and mutation classes.~~
- [ ] ~~2.3 Bind request decisions to exact environment/origin/method/template/rule/source generation without persisting concrete parameters.~~

## 3. Make causality and transport enforcement total

- [ ] ~~3.1 Replace fixed action sleeps as authority with unique action/navigation generations that close only after deterministic bounded settlement.~~
- [ ] ~~3.2 Propagate/refuse generation identity across delayed callbacks, redirects, frames, popups, dedicated workers, and concurrent actions.~~
- [ ] ~~3.3 Require the same admission at Playwright route, CDP Fetch, WebSocket, API relay, and L5; layer disagreement or missing capability hard-fails.~~

## 4. Adversarial and mutation proof

- [ ] ~~4.1 Test delayed 249/250/251 ms requests, timer/microtask/event callbacks, redirect codes/method preservation, navigation bootstrap, concurrent generations, popup/worker traffic, and stale registries.~~
- [ ] ~~4.2 Test exact admitted reads still work while unknown/mutation/ambiguous/unproven requests open zero upstream connections and produce categorical evidence.~~
- [ ] ~~4.3 Register mutations for passive-unknown relabeling, early generation close, host-only CDP/proxy fallback, missing redirect binding, stale proof, and incomplete consumer census.~~

## 5. Acceptance and handoff

- [ ] ~~5.1 Run focused endpoint/journey/browser/proxy safety suites, typechecks, hardening/mutations, local/clean/topology gates, and the full regression without real target access.~~
- [ ] ~~5.2 Update safety/architecture/decision truth, strict-validate, inspect privacy/diff, and integrate only through an owned C-00 session.~~
