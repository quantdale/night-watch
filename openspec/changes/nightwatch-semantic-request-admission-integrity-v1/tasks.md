Implementation was originally outside the planning-only audit campaign that created this change. The umbrella campaign `nightwatch-priority-audit-remediation-sequence-v1` (M5, owner-authorized) performed the implementation; each item records evidence from the live checkpoint. The planning-only task STATE/REPORT remain historical planning records (never rewritten); implementation truth lives in the umbrella task per the recorded continuity decision.

## 1. Establish the egress authority denominator

- [x] 1.1 Syntax-aware discovery of every browser route/CDP/WebSocket continuation, redirect path, API relay, proxy tunnel, endpoint registry producer, and journey intent/settlement consumer: `bin/lib/transportEffectCensus.mjs` (+ `.d.mts`) — closed effect vocabulary and authority classes, disjoint registry roots, UNKNOWN_EFFECT/STALE/DUPLICATE/EMPTY fail-closed, deterministic digest. Live census 55 sites / 55 classified (SEMANTIC_ADMISSION 5, PROXY_UPSTREAM_DIAL 2, CONTAINED_ENVELOPE 1, SEMANTIC_COMPOSED 2, LOCAL_LOOPBACK_CLIENT 2, MANUAL_LANE 5, TEST_FIXTURE 38), digest `sha256:7b709791b474427ea40b297c`, enforced by `checkSemanticTransportTotality` with floors (siteCount ≥ 40, classified == total, SEMANTIC_ADMISSION ≥ 4).
- [x] 1.2 Focused regressions for the three cited defects: `tests/unit/semanticRequestAdmission.test.ts` reproduction pins (unclassified initialization continued via the PASSIVE relabel; `ACTION_SETTLE_MS = 250` causality ordering; CDP handler URL-only + host-allow continue) — each pin was committed green first, then INVERTED by the wiring with ordering/absence proofs (A/B/C/D all inverted).
- [x] 1.3 Schemas defined and declared: `nightwatch.semantic-admission.v1` (immutable handles + categorical refusal receipts, 15-code closed union with inline rationale for the two removed unreachable codes), `nightwatch.causal-generations.v1`, `nightwatch.bootstrap-exemption.v1`, `nightwatch.admission-ticket.v1` — all in the schema lifecycle.

## 2. Implement pre-effect semantic admission

- [x] 2.1 Exact source/provenance/currentness-bound KNOWN_READ proof before any product API request continues: pure `evaluateAdmission` is THE single decision whenever a registry is bound (the L1 observer gate delegates to it wholesale — method-drift, mutation-twin, and family analysis produce precise codes at every transport); source proof/currentness bound per rule binding; stale snapshot (`admissionSourceCurrent === false`) refuses everything. E2E proves the admitted read reaches loopback and the unproven one does not.
- [x] 2.2 Passive-unknown replaced by finite exemptions: `bootstrapExemptions.ts` (GET/HEAD-only enforced at registration, anchored routes, exact env/origin/method family, navigation-generation-scoped, count-budgeted, proof+currentness-bound; two-stage MISMATCH vs UNREGISTERED). Unknown/stale/ambiguous/method-drift/mutation refuse — pins A and C inverted (`PASSIVE_UNKNOWN_OBSERVED` survives only as an assigned diagnostic label, never a compared condition; rule-pinned).
- [x] 2.3 Decisions bind environment, origin, method, proven template (`<RULE:id>` marker or exact static path — M4 reuse, no second route system), rule/proof identity, source currentness, generation, transport, and admission identity; transient match patterns are kept OFF handles and receipts (persistence-safe: asserted by tests that handles/refusal receipts contain no path material).

## 3. Make causality and transport enforcement total

- [x] 3.1 Fixed sleeps replaced as authority: generations open on beginJourneyIntent / main-frame navigation and settle ONLY at deterministic bounded settlement (engine: after required-network + structural in a finally, success and failure; navigation: loaded-and-drained window with a hard re-arm cap; exploration's stronger hold preserved). `ACTION_SETTLE_MS` remains pacing only, documented and rule-pinned; pin B inverted.
- [x] 3.2 Propagation/refusal across delayed callbacks, redirects, frames, popups, dedicated workers, concurrent actions: redirect follow-ups attribute ONLY through the CDP-backstop provenance chain (`redirectedRequestId` → original URL → earning generation; unchainable ⇒ AMBIGUOUS); cross-generation confusion matrix at pure level (A/B non-exchange, implicit AMBIGUOUS, reuse-after-close, closed-handle replay) plus the browser-level slow-redirect closure E2E; frames/popups/workers covered structurally by context-wide interception + popup auto-wiring + blocked service workers (existing coverage) — dedicated per-context admission assertions remain a named PARTIAL (see 4.1).
- [x] 3.3 Same admission at Playwright route, CDP Fetch, WebSocket, API relay, and L5: L1/L2/L0 share one gate (admitRequest); layer OWNERSHIP prevents any double-spend (L1 non-redirect, L0 redirect follow-ups, L2 sockets); L5 consumes one-shot bounded tickets (`admissionTickets.ts`) with per-host CONNECT capability+cardinality; relay composes (caller authority AND semantic admission, dev-mode mandatory); `consumeAdmission` fails closed with `ADMISSION_TRANSPORT_DISAGREEMENT`; totality enforced by `checkSemanticTransportTotality`.

## 4. Adversarial and mutation proof

- [x] 4.1 Delayed 249/250/251 ms (pure timing-independence: admitted throughout, authority ends only at settle, never-open is closed at t=0), redirect codes 301/302/303/307/308 with ACTUAL Chromium method behavior (GET lands for all five; POST converts for 301/302/303 and is refused preserved-POST for 307/308 with dst upstream counters), navigation bootstrap E2E (in-budget grant once; unexempted/over-budget/post-settlement zero upstream), concurrent generations (pure + slow-redirect browser closure), stale registries (snapshot divergence ⇒ DISAGREEMENT). [PARTIAL: explicit microtask/timer/event-callback request-source tests and dedicated popup/frame/worker admission assertions were not written separately — their authority path is the same synchronous interception gate covered above; recorded rather than claimed.]
- [x] 4.2 Exact admitted reads work (bootstrap E2E, WS E2E, redirect matrix landings, relay valid+valid row, proxy minted forward) while unknown/mutation/ambiguous/unproven requests open ZERO upstream connections: counters asserted at every boundary (fixture `hits`, fixture `wsConnections`, redirect dst counters, proxy `requestCount`/`connectionCount`, relay `fetchCalls`) with categorical receipts only (no path/query material — asserted).
- [x] 4.3 Mutations registered and DETECTED (full campaign: rules=85 probes=143 detected=143 undetected=0 restored=100): passive-unknown relabel reintroduction ⇒ HC-132; missing L1 check ⇒ HC-131; early/generation-close weakening ⇒ HC-134 (+ engine settlement-order rule pin); host-only CDP fallback ⇒ HC-135 (+ HC-133 method drop); WS bypass ⇒ HC-136; proxy capability bypass ⇒ HC-137; relay composition bypass ⇒ HC-138; incomplete/ drifted transport census ⇒ HC-130. Stale REGISTRATION is HC-130; stale CURRENTNESS refusal is proven behaviorally by the pure matrix row + snapshot-divergence test (no source mutation probe — recorded, not claimed).

## 5. Acceptance and handoff

- [x] 5.1 Validation run for this checkpoint: focused suites green (admission suites 21/21; 17-file battery 171/171 incl. journey/observers/harnesses/all five smokes/proxy/relay; 20-file affected battery 190/190 earlier); `npm run typecheck`, `typecheck:bin`, `schema:check`, `hardening:check`, `validation:universe`, `agent/handoff/project/workspace/session` checks all PASS; full probe campaign 143/143; strict OpenSpec PASS for this change and the umbrella; `npm run gate:dev` PASS (393 affected, all step exits 0); `npm run gate:milestone` PASS (twelve steps exit=0 at `70104a00`, hardening-rules 162.4s). Full regression / clean / topology gates belong to umbrella milestone M6 final certification and are not claimed here.
- [x] 5.2 docs/SAFETY_MODEL.md and docs/ARCHITECTURE.md audited for staleness against the new admission model — no contradicting prose (SAFETY_MODEL's "passive" language refers to the declarative ACTION classifier, a different mechanism; no document claims PASSIVE_UNKNOWN_OBSERVED grants effect authority or that the 250 ms window is a security boundary); this change's design/tasks + umbrella STATE decisions carry the behavior documentation; strict validation PASS; privacy/diff inspected; synthetic/loopback/`.invalid` only, no real target traffic, no external publication. C-00 integration is owned by the umbrella campaign (M6).
