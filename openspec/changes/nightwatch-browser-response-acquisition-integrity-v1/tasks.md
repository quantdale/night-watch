Implementation is outside the planning-only audit campaign. These tasks are declared not in scope; none has been performed.

## 1. Freeze acquisition behavior

- [ ] ~~1.1 Add contained oversized, compressed, chunked, stalled, late-resolution, and teardown-race fixtures.~~
- [ ] ~~1.2 Inventory every response-body caller, timeout, recorder, semantic hook, and context settlement path.~~

## 2. Implement bounded ownership

- [ ] ~~2.1 Add byte/deadline acquisition budgets and preflight length refusal.~~
- [ ] ~~2.2 Replace or fail closed around whole-buffer non-cancellable APIs.~~
- [ ] ~~2.3 Register, abort, and join every acquisition with the owning context generation.~~
- [ ] ~~2.4 Discard partial bytes and emit only categorical incomplete evidence.~~

## 3. Prove closure

- [ ] ~~3.1 Add resource-leak, property, and mutation coverage for cancellation, byte caps, and teardown joins.~~
- [ ] ~~3.2 Run contained browser tests, typecheck, hardening, local/clean gates, and full regression without external targets.~~
