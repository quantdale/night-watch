# Audit — W12 current-source unknown-defect yield

Audited at live starting SHA `4e763f3f079863a262906a8b134539e309c8d054`
before any W12 provider probe or investigative call.

## Predecessor and authorization

W11 (`nightwatch-autonomous-yield-proof-w11-v1`) is preserved as a frozen
successor predecessor: historical strict EXACT was `0/13` with zero leakage,
while its unknown-source arm was invalid on six frozen-provider timeouts and
produced no yield. The owner-supplied W12 execution prompt authorizes a new
task, new provider selection, fresh census, fixed broad/scoped matrix,
mechanical yield measurement, and C-00 integration. W11's historical arm is
not re-run by this change.

## Live Git and workspace

Canonical `main` and `origin/main` both resolve to
`4e763f3f079863a262906a8b134539e309c8d054` at activation. The W12 worktree
is owned by C-00 at
`session/nightwatch-current-source-unknow-75aee275`. Four pre-existing stale
worktrees remain owner-attention records; W12 does not adopt, release, remove,
or modify them.

## Existing capability boundary

W12 composes the existing approved eight-repository source universe, source
currentness checks, diverse source index, bounded reasoner loop, contained
reproduction providers, dossier/admission gates, leakage/redaction controls,
and owner-local persistence. No new network, shell, product, database,
credential, sibling-write, or publication authority is granted by this
change.

## Required execution audit

Before the first investigative call, the task MUST add and commit the provider
selection policy and W12 machine-readable freeze. During and after execution,
the report MUST cite exact provider/run receipts, census and identity
snapshots, fingerprint integrity probes, candidate/admission/refusal proofs,
novelty receipts, safety counters, validation receipts, and C-00 integration
evidence. Provider-blocked or incomplete evidence MUST remain categorical and
MUST NOT be published as zero yield.
