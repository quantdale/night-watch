## 1. Campaign setup and baseline

- [x] 1.1 Create and activate the continuity-v2 successor task with explicit verdict effect `PRESERVE`, current Git anchors, safety scope, and OpenSpec binding.
- [x] 1.2 Run and record the authoritative local baseline, including quality-gate, semantic, owner-provenance, synthetic campaign, Control Center, continuity, project, handoff, and inventory results.
- [x] 1.3 Audit current replay, observation, campaign, clustering, state-checker, cache/property, chaos, auth, and diagnostics implementations; create a defect ledger and bounded reproduction matrix.

## 2. Replay reliability and identity

- [x] 2.1 Reproduce the current Phase 2C strict-replay divergence path with sanitized deterministic fixtures and identify whether the owning defect is timing, capture ordering, state, environment, product, or identity.
- [x] 2.2 Add the bounded replay observation ledger and categorical divergence classifier with strict unknown/non-success behavior.
- [x] 2.3 Audit canonical identity fields and implement only explicitly justified canonicalization; preserve meaningful order, multiplicity, routes, methods, oracle, semantic, and source identity.
- [x] 2.4 Add metamorphic replay tests for permutations, timing, telemetry, polling, equivalent DOM/order, meaningful changes, unsupported values, cycles, and privacy bounds.
- [x] 2.5 Add regression coverage proving replay diagnostics and fingerprints are deterministic across repeated processes and do not leak raw observations.

## 3. Campaign yield, prioritization, and findings

- [x] 3.1 Backtest the existing planner against the current mechanically proven inventory and measure selection redundancy, age, semantic density, relation density, anomaly history, replay confidence, and cost.
- [x] 3.2 Implement or repair a versioned explainable prioritization score using only bounded safe inputs and stable tie-breakers.
- [x] 3.3 Add diversity-aware selection constraints and prove deterministic non-starvation across repository, route/entity, semantic, journey/interface, and source-change dimensions.
- [x] 3.4 Add yield attribution that separates volume, coverage quality, detections, replay/minimization confidence, false positives, and stable clusters; preserve `NO_SAFE_NEW_FAMILY` when applicable.
- [x] 3.5 Audit and strengthen finding fingerprint/cluster identity, idempotent occurrence updates, product-vs-framework classification, and sanitized dossier evidence.
- [x] 3.6 Add repeated-anomaly and benign-control tests covering timing/payload/order/polling/optional/empty/pagination/redirect variation without precision loss.

## 4. Explicit project-verdict and continuity protocol

- [x] 4.1 Replace task-name-based accepted-verdict inference with an exact bounded `PROJECT_VERDICT_EFFECT` field and semantic validation for `PRESERVE`, `REEVALUATE`, and `SUPERSEDE`.
- [x] 4.2 Migrate only the active successor and directly relevant current fixtures; preserve all historical task records and the existing `OPERATIONALLY_ACCEPTED` project verdict.
- [x] 4.3 Refactor continuity parsing to consume canonical structured fields and stop deriving milestone/status state from incidental narrative vocabulary.
- [x] 4.4 Harden live documentation truth checks for task/phase/status/effect/next-action/completion contradictions while allowing marked historical prose.
- [x] 4.5 Add negative and compatibility tests for missing, duplicate, malformed, mismatched, premature, requalification, supersession, and task-name-independent verdict effects.

## 5. Persisted execution, auth, diagnostics, and quality audits

- [x] 5.1 Expand deterministic interruption/resume chaos coverage across manifest/evidence/checkpoint/teardown boundaries; prove no loss, duplication, corruption, or false completion.
- [x] 5.2 Expand synthetic auth lifecycle coverage for start/mid-run expiry, malformed/missing/wrong-environment state, refresh, human-required, and replay invalidation with sanitized classifications.
- [x] 5.3 Add high-value bounded replay/campaign diagnostics for action/replay/oracle/source/contract/work-item/state-transition identities without payloads or credentials.
- [x] 5.4 Reprofile eligibility, prioritization, clustering, replay lookup, contract joins, currentness, and Control Center reducers; optimize only measured deterministic hot paths.
- [x] 5.5 Audit the cache matrix and canonical-digest property suite so every case changes the authoritative input it claims to test and every property asserts the actual invariant.

## 6. Bounded real operation and reproducibility

- [x] 6.1 Evaluate the owner-managed DEV authentication branch and, when it is valid, run the bounded serial Phase 2C repetition sample; classify every outcome without hiding divergence behind retries. The current state was auth-blocked before browser-context creation, so the actual-observation count is zero.
- [x] 6.2 Reconcile the bounded real-operation evidence and safe local interruption/resume coverage; current DEV Phase 4/5/campaign/replay re-observation was not authorized past the same auth gate, and prior accepted evidence remains historical rather than being relabeled.
- [x] 6.3 Run the complete local quality matrix, Control Center checks, clean Node20 gate, and canonical/topology-correct isolated parity with exact counts and skip identities.
- [x] 6.4 Inspect external Actions once near the release checkpoint and classify executed-step evidence versus zero-step platform/billing non-evidence.

## 7. Closure and checkpoint

- [x] 7.1 Repair all Critical/High defects discovered in the ledger and rerun their regressions and affected real/synthetic paths.
- [x] 7.2 Update decisions, architecture/current-state/roadmap snapshots, task STATE/REPORT/PLAN, and execution prompt with exact evidence, limitations, and recommended successor.
- [x] 7.3 Run final privacy/diff/hardening/agent/project/handoff checks, commit a validated durable checkpoint, verify `main == origin/main`, and leave the tree clean.

## Closure evidence

The DEV branch stopped at `HUMAN_AUTH_ACTION_REQUIRED` after all local safety
gates passed and before browser-context creation. This is a truthful external
evidence limitation, not a replay pass, product finding, or Nightwatch
failure. The local and isolated release evidence is recorded in the active
task STATE/REPORT, and `OPERATIONALLY_ACCEPTED` remains preserved.
