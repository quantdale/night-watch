Implementation was originally declared outside the planning-only audit campaign that created this change. The umbrella campaign `nightwatch-priority-audit-remediation-sequence-v1` (M3, owner-authorized) later performed the implementation; each item below records implementation evidence. Planning completion and implementation completion are distinct: the original planning-only STATE remains readable as planning history and is not rewritten.

## 1. Establish the structural boundary

- [x] 1.1 Census completed: recon census (planning) plus the mechanically generated `bin/lib/privateConsumerCensus.mjs` — non-zero (40 consumers; 14 production; 10 production writers; 26 screening call sites), digest `sha256:<24>`, closed registry with unknown/stale/duplicate/capability-bypass/empty fail-closed codes; enforced by `checkPrivateSurface` (hardening) and proven by `tests/unit/privateConsumerCensus.test.ts`.
- [x] 1.2 Focused regression `tests/unit/privateStructuralScreening.test.ts` proves ordinary quoted objects are refused structurally and by repaired text defense, including the compound-identity regression (`billing_group_id`, `payer_id`, `email_address`, `customer_name`, `tokens` — reproduced failing against live source before the fix).
- [x] 1.3 Closed SENSITIVE_PRIVATE_KEYS / numeric-ok cost-amount set plus the closed `SENSITIVE_KEY_SUFFIXES` identity-suffix vocabulary, bounded walk (depth/nodes/keys; array elements counted as nodes so NODE_BUDGET is reachable), categorical PrivateStructureFailure codes.

## 2. Implement structural validation

- [x] 2.1 findStructuralPrivateFailure: sensitive keys (incl. compound suffixes), prototype/accessor/cycle/depth/node/key budgets; store refuses before file creation (`PRIVATE_ARTIFACT_PRIVACY_BLOCKED`, no file written — asserted).
- [ ] ~~2.2 Replace generic unknown store inputs with family-specific safe DTO constructors and independent write-time validation.~~ [PARTIAL: store write-time structural+text admission landed (`assertPrivatePayload` → `containsPrivatePayload`) and every reader family already revalidates with its own validator (reviewStore envelope validators, selfDev `validate*`, run-evidence strict parsers, findings `validateArtifact`). Full per-family branded DTO constructors with exact-key schemas remain deferred; NW-AUD-018 (Phase 4) builds the family-profile typed-firewall infrastructure for authenticated evidence first, and this change's structural primitives are its base. Not checked because the branded-DTO migration itself did not land.]
- [x] 2.3 PRIVATE_VALUE_RE accepts optional JSON quotes between label and separator, the closed compound identity-suffix class, and array-valued labels; customer/account/billing-group/payer/email/token aliases covered; structure remains primary. Canonical text defense: NFKC + zero-width stripping (`canonicalizePrivateText`).

## 3. migrate readers and consumers

- [x] 3.1 All PrivateArtifactStore writers pass through assertPrivatePayload structural+text; C-10 firewall retains structural primary (digest/walkNode re-derivation) + text defense backstop.
- [x] 3.2 runEvidenceReader structural-checks parsed JSON (`containsStructuralPrivateShape(parsedValue)`); findingsAuthority now ALSO structurally revalidates parsed JSON (`containsStructuralPrivateShape(raw)`) — previously text-only on raw bytes, which JSON-escaped labels defeat (HC-115/HC-118 prove both non-vacuously).
- [x] 3.3 Generated non-zero consumer census with reject-unknown/stale/duplicate/bypass: `bin/lib/privateConsumerCensus.mjs` (+ `.d.mts`), closed registry in-module, wired into `checkPrivateSurface` with floors (consumers ≥10, production ≥8, writers ≥6, screening calls ≥5, digest shape), synthetic negative tests for every failure code, probe HC-116.

## 4. Adversarial and mutation proof

- [x] 4.1 Ordinary values under quoted, nested, array, alias, case/separator, compound-suffix, encoded (base64/URL values under sensitive keys), escaped (`tok\u0065n`), Unicode (fullwidth, zero-width), and duplicate-key (parsed + raw text) forms — all refused; near-miss keys (`author`, `authorized`, `accountability`, `tokenizerMode`, `secretary`) proven non-sensitive.
- [x] 4.2 Malformed JSON labeled text refused; prototype/accessor/cycle categorical; width (10 001 keys ⇒ KEY_BUDGET_EXCEEDED) and node (10 002-element array ⇒ NODE_BUDGET_EXCEEDED) budgets categorical; oversized payload prefix-bound scan documented by test; tampered persisted artifact refused at reader admission; historical schema (legacy proxy summary migration) covered by `controlCenterRunEvidenceReader.test.ts`; categorical no-echo asserted (error message equals the code, never the payload).
- [x] 4.3 Mutations registered and DETECTED (probe campaign `rules=1 probes=11 detected=11 undetected=0 restored statusUnchanged=true`): regex-only admission ⇒ HC-112; missing recursive key checks ⇒ HC-114; permissive unknown keys ⇒ NOT APPLICABLE while 2.2's exact-key admission is deferred (there is no unknown-key protection to mutate; recorded rather than faked); missing writer/read revalidation ⇒ HC-112 (writer), HC-115 (findings reader), HC-118 (run-evidence reader); incomplete census ⇒ HC-116 (registry drift); sentinel-only corpora ⇒ HC-111 + HC-113 with the ordinary non-sentinel corpus in 1.2/4.1; plus HC-005, HC-117 (canonicalization), HC-119 (suffix vocabulary), HC-120 (compound label RE).

## 5. Acceptance and handoff

- [ ] ~~5.1 Run focused private artifact/triage/review/selfDev/Control Center/production firewall suites, typechecks, hardening/mutations, continuity/workspace/project checks, local/clean/topology gates, and full regression.~~ [PARTIAL — M3 scope proven: focused suites (privateStructuralScreening, privateConsumerCensus, privateArtifactAtomic, controlCenterFindingsAuthority, controlCenterRunEvidenceReader, reviewStore*, aiReview, campaign, privateTriage, c10ProductionProjection, phase17EvidenceHardening, evidence*, selfDev*, changeIntelligence) PASS; `npm run typecheck` PASS; `checkPrivateSurface` rule PASS; probe campaign 11/11 DETECTED; `npm run gate:dev` PASS; `npm run gate:milestone` PASS — receipts recorded in the umbrella STATE validation ledger. Two test-environment defects found en route and root-caused: selfDevAdoptionCli assumed a clean authoritative checkout (now branches on the product's own trust gate), and evidenceRetention asserted exact directory counts under concurrent lanes (now asserts the removal-direction subset). Pre-existing load flakes (evidenceRetention under parallel artifacts/ writers; l6Containment bwrap timing) recorded in STATE, pass isolated. NOT checked here: full regression / clean / topology gates belong to umbrella milestone M6 final certification and are not yet run at M3.]
- [x] 5.2 SAFETY_MODEL.md and ARCHITECTURE.md audited for staleness against the new behavior — no contradicting prose found (ARCHITECTURE's `containsPrivatePayloadShape` reference remains true); this change's design/tasks carry the privacy documentation; strict `openspec validate nightwatch-private-payload-screening-structural-integrity-v1 --strict` PASS; privacy/diff inspected; synthetic values only, no real private data, no external publication. C-00 integration is owned by the umbrella campaign (M6) through the single session worktree, not by this planning change's original closure.
