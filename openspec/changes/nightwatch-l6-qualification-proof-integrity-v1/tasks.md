Implementation is explicitly outside the planning-only audit campaign that created this change. These tasks are declared not in scope for the current task; none has been performed.

## 1. Define the proof boundary

- [ ] ~~1.1 In a separately authorized implementation session, inventory every L6 proof field, probe, observer, executable lookup, capability constructor, assertion, and authenticated consumer.~~
- [ ] ~~1.2 Add regressions demonstrating the omitted UDP predicate, unused browser listeners, suppressed speculative stimulus, plain-object READY fabrication, and qualification-to-launch substitution.~~
- [ ] ~~1.3 Define closed schemas for probe manifests, witnessed outcomes, runtime identities, qualification generations, consumable handles, revocation, and blocker codes.~~

## 2. Implement non-vacuous qualification

- [ ] ~~2.1 Replace ad hoc aggregate conditions with a total decision table requiring every manifest entry and rejecting unknown, duplicate, skipped, timed-out, or ambiguous evidence.~~
- [ ] ~~2.2 Add working positive controls for Node DNS/TCP/UDP/HTTP(S)/descendant probes and verify observer reachability before containment assertions.~~
- [ ] ~~2.3 Rebuild browser fixtures to actively exercise DNS, direct TCP, HTTP(S), upgrade, UDP/QUIC, speculative/background, and descendant traffic without suppressing the stimulus.~~

## 3. Bind qualification to execution

- [ ] ~~3.1 Capture safe exact identities for Bubblewrap, Node, Chrome, bootstrap/control code, launch policy, host capability, and target identity class.~~
- [ ] ~~3.2 Return an opaque bounded-freshness handle; consume/revalidate it at authenticated launch before workspace or secret-bearing state creation.~~
- [ ] ~~3.3 Make READY construction private, add a mechanically isolated test factory, and reject plain/stale/reused/foreign capabilities.~~

## 4. Adversarial, privacy, and mutation proof

- [ ] ~~4.1 Test executable/path/inode/digest/permission swaps, symlink and rename races, handle expiry/reuse, policy drift, concurrent qualification, process death, and cleanup failure.~~
- [ ] ~~4.2 Test every positive control and contained outcome, including IPv4/IPv6/mapped addresses, TCP/UDP DNS, QUIC, descendants, relay loss, and unsupported hosts.~~
- [ ] ~~4.3 Inject secret/path/customer sentinels and unbounded diagnostics; prove receipts/errors remain categorical and bounded.~~
- [ ] ~~4.4 Register mutations for every proof predicate, probe wiring, listener target, resolver flag, constructor restriction, identity check, and revocation step.~~

## 5. Acceptance and handoff

- [ ] ~~5.1 Run focused L6/OOPS/process suites, typechecks, hardening/mutations, synthetic campaign, continuity/workspace/project checks, local/clean/topology gates, and full regression on supported and unsupported hosts.~~
- [ ] ~~5.2 Update architecture/safety/campaign receipt documentation, strict-validate this change, inspect privacy/diff, and integrate only through an owned C-00 session with no authenticated or external target contact.~~
